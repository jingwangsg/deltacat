{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "5 - DeltaCat Lance 读路径 & Shard 实测",
  "description": "Chapter 5 — 为什么 Lance 的读被绕过 DeltaCat,三种读 pattern,以及 shard × worker 实测结论",
  "overview": "# Tour · Lance 读路径 & Shard 实测\n\n第 4 章讲过 DeltaCat 的「标准读路径」:`TABLE_TYPE_TO_READER_FUNC` 里注册好的 reader 函数 + `download_manifest_entries` 分发。本章回答一个问题:**为什么 Lance 的读不走这条路?** 以及给出一个你真上生产时会用到的 **shard 数量公式**。\n\n配套的具体数字、调用形状、函数签名都在各个 step 里;本页只讲大局。\n\n---\n\n## 1. 核心问题:Lance 读目前没 DeltaCat 原生 dispatch\n\n翻开 `deltacat/types/tables.py` 第 107 行,你会看到这个 registry:\n\n```python\nTABLE_TYPE_TO_READER_FUNC: Dict[str, Callable] = {\n    DatasetType.PYARROW_PARQUET.value: pa_utils.file_to_parquet,\n    DatasetType.PYARROW.value:         pa_utils.file_to_table,\n    DatasetType.PANDAS.value:          pd_utils.file_to_dataframe,\n    DatasetType.NUMPY.value:           np_utils.file_to_ndarray,\n    DatasetType.POLARS.value:          pl_utils.file_to_dataframe,\n}\n```\n\n**注意没有 `DatasetType.LANCE` 项**。第 3 章里已经造好的两个 helper `read_lance_table`([#5])和 `open_lance_dataset`([#6])也**没有**被连到这张表。\n\n`experiments/02_read_local.py` 的 top docstring 直接坦白了这件事(见 [#4]):\n\n> The open-source deltacat doesn't have Lance-aware read dispatch yet, so we resolve manifest entries (paths) from the catalog, then read the Lance datasets directly.\n\n**这不是 bug,是设计选择。** Lance 的卖点是 lazy + O(1) 随机访问 + 列投影 —— 用户要的是 `ds.take([12, 47, 203])` 这种调用形状,一旦走进「 download_manifest_entries 返回一个 PyArrow Table」的漏斗,lazy 的优势就没了。所以 DeltaCat 留了一个 gap,让用户拿 manifest 里的路径后直接 `lance.dataset(path)`。\n\n参考 Chapter 4 讲 `TABLE_TYPE_TO_READER_FUNC` dispatch 的步骤 —— 本章的 [#3] 就是在那张表上「找不到 LANCE」的跨章呼应。\n\n## 2. 标准读 vs Lance 实际读\n\n| 维度 | 标准读路径 (Chapter 4) | Lance 实际读 (Chapter 5) |\n|---|---|---|\n| 入口 | `dc.read_table(...)` / `download_manifest_entries` | 用户代码直接 `lance.dataset(path)` |\n| 返回物 | PyArrow Table / Pandas / Polars ...(eager) | `lance.LanceDataset` 句柄(lazy) |\n| 读什么 | 整个 manifest 拼起来 | 用户自选:全读 / take 几行 / 只某几列 |\n| 适合场景 | OLAP 扫描、小表全载入 | 随机采样(训练)、metadata-only 查询 |\n| 绕过的东西 | 无 | DeltaCat 不参与数据层,只负责 catalog 路径解析 |\n\n## 3. 三种读 Pattern 的决策树\n\n来自 `experiments/02_read_local.py`([#7]–[#10]):\n\n```\n                    需要读 Lance 数据?\n                          │\n         ┌────────────────┼────────────────┐\n         │                │                │\n   数据能全塞进内存?   按索引随机采样?   超大数据+有 Ray 集群?\n         │                │                │\n         ▼                ▼                ▼\n   Pattern A: Eager   Pattern B: Lazy   Pattern C: Distributed\n   lance.dataset(p)   merge 成单 ds      Ray worker 分片并行\n   .to_table() 全加载  ds.take([...])    每 worker 读一部分\n                      O(1) 随机访问\n                      columns=[...] 投影\n         │                │                │\n         ▼                ▼                ▼\n   一次性 OLAP       训练 dataloader     生产规模读 35GB+\n                    推理按索引取样\n```\n\n本章用到的小数据集:`feb2_small_batch` —— 344 个 Lance 目录、7.15 GB(视频 blob 占大头)。三种 pattern 在本地 / S3 的实测值([#11] 有完整对照表)。\n\n## 4. Pattern B 为什么能 O(1)\n\nPattern B 的核心一行是 `ds.take([0, 100, 200])`([#8]),本地 0.34s、S3 7.9s。能这么快的前提是:**Lance dataset 的 fragment 粒度合理**。\n\n回看第 3 章的 `write_lance_table`(`lance_utils.py:488`),默认 `max_rows_per_fragment = 100_000`。Lance 的索引结构是「row number → (fragment_id, offset)」,`take([i])` 只要:\n\n1. 从 row number 定位到 fragment(manifest 读 once,缓存);\n2. 打开那个 fragment 的数据文件(本地 mmap / S3 GET range);\n3. 跳到 offset 处 decode 一行。\n\n所以每行 `take` 的成本 ≈ 一次文件定位 + 一次 row decode,**与总行数无关** —— 这是 O(1)。对比 Parquet:Parquet 的 row group 默认 1M 行,随机读一行要整个 row group 解压,成本是 O(row_group_size)。\n\n参考 Chapter 3 里讲 `write_lance_table` `max_rows_per_fragment=100_000` 设计的那一步 —— 本章 [#8] 就是在那个「小 fragment」决定上拿分的地方。\n\n## 5. Shard × Worker 实测(experiment.md 的核心产出)\n\n三个数据集的**最优点**惊人一致:**shards ≈ workers,而不是 `ceil(bytes/512MB)`**。\n\n| 数据集 | 大小 | 行数 | 行大小 | 最优 shards × workers | 最快时间 | 峰值吞吐 |\n|---|---|---|---|---|---|---|\n| `feb2_small` | 7.15 GB | 344 | ~20 MB(含 video) | 20 × 50 | 14.6s (2.4x) | 488 MB/s |\n| `feb_08_500hr` | 35 GB | 54M | ~650 B | 5 × 5 | 19s (10.2x) | 2.3 GB/s |\n| `mar_6_umi` | 18.5 GB | 54M | ~340 B | 5 × 10–20 | 4.6–4.8s (15–16x) | **3.9 GB/s** |\n\n**三个核心发现:**\n\n1. **最优点不是 `ceil(bytes / 512MB)`** —— 按那个公式 35GB 要 70 shards,但 feb_08 实测 **5 shards + 5 workers = 19s(10.2x)**;100 shards + 5 workers 反而是 **182s(9.6x 慢)**,因为 worker 数不够,shard 多出来的 S3 per-object 开销(每 shard 固定 ~3–8s 的 manifest 读 + fragment 元数据解析)白白堆积。\n2. **行越小,S3 吞吐越高** —— 大 blob(20 MB/行,视频)让 per-GET 开销摊薄不下来,feb2_small 卡在 488 MB/s;小行(340 B)能把 S3 带宽打到 3.9 GB/s。\n3. **存在 S3 聚合带宽上限** —— feb_08 ≈ 2.3 GB/s,mar_6 ≈ 3.9 GB/s。超过这个点,加再多 worker 也没用。\n\n详见 [#11]–[#14] 四步。\n\n## 6. Shard 数量速查表\n\n| 想做什么 | 应取的 shards 数 | 参考 step |\n|---|---|---|\n| 大数据集(>10 GB)生产读 | `max(min_shards, num_training_workers)`,通常 5–20 | [#14] |\n| 训练 8 GPU,每 GPU 一个 dataloader worker | 8(不是按字节) | [#14] |\n| 小数据集(<1 GB) | 1 个 shard,多 worker 抢读没意义 | [#11] |\n| 按笔记公式 `ceil(bytes/512MB)` | ⚠️ 太多了,仅适合 compaction 内部 hash bucket,不适合输出文件数 | [#14] |\n\n## 7. 常见困惑 & 对应跳转\n\n| 现象 | 根因 | 跳到 |\n|---|---|---|\n| `dc.read_table(...)` 对 Lance 报错 / 走错路径 | Lance 没在 `TABLE_TYPE_TO_READER_FUNC` 里 | [#3] |\n| `read_lance_table` 明明存在,为什么没被调用 | 造了但没连进 dispatch | [#5] |\n| Pattern B 的 `take([0,100,200])` 为什么这么快 | fragment 100K 行 + Lance row-number 索引 | [#8] |\n| 100 shards 比 5 shards 还慢 | Per-shard 固定开销(manifest + metadata ~3–8s) | [#12] |\n| 加 worker 到 50 个,速度不再提升 | 撞了 S3 聚合带宽上限(feb_08 ~2.3 GB/s) | [#12] |\n| video 数据吞吐只有 488 MB/s | 行大(20 MB),per-GET 开销摊不薄 | [#14] |\n\n---\n\n**读完本章,你会:**\n\n- 理解 DeltaCat 为什么**刻意**在 Lance 读路径上留了 gap(答案:lazy 语义不能包进 eager 的 `download_manifest_entries`);\n- 能在三种读 pattern 里对号入座(答案:A 全载入 / B 随机访问 / C 分布式);\n- 写 compaction 时知道 shard 数应该设多少(答案:`max(min_shards, num_training_workers)`,不是 `ceil(bytes/512MB)`)。\n\n下一章可以读调度层(Ray actor / `download_manifest_entries` 内部),看 Chapter 4 的 dispatch 如何细粒度并发执行。",
  "steps": [
    {
      "title": "导读: 第 5 章在讲什么",
      "description": "**核心结论:** Chapter 4 讲过「DeltaCat 标准读路径」(PyArrow/Pandas/Polars 走 `TABLE_TYPE_TO_READER_FUNC` dispatch),但 **Lance 不在那张表里**。本章回答三个问题:gap 为什么存在、Lance 自己有哪三种读 pattern、生产环境 shard 数到底该设多少。\n\n**本章的三条主线:**\n\n1. **Gap 论证(3 步)** — `DatasetType.LANCE` enum 的注释明说它是 lazy 句柄;`TABLE_TYPE_TO_READER_FUNC` 字典里没有 LANCE 项;`experiments/02_read_local.py` 的 docstring 直接坦白。\n2. **三种读 pattern(6 步)** — Pattern A 全载入、Pattern B lazy O(1)、Pattern C Ray 分布式。每一种的调用形状、适合场景、本地 / S3 实测数字。\n3. **Shard 实测(4 步)** — 三个数据集的 shard × worker 矩阵、跨数据集对比、对笔记公式 `ceil(bytes/512MB)` 的修正。\n\n**本章用到的两个实验脚本:**\n\n| 脚本 | 规模 | 用途 |\n|---|---|---|\n| `experiments/02_read_local.py` | 344 Lance 目录,7.15 GB | 三种 pattern 在本地 / S3 的直接对比 |\n| `experiments/10_read_scaling_s3.py` | 344 个分片,35 GB,54M 行 | shard 数 × worker 数矩阵,生产规模 |\n\n**与前三章的关系:** 第 3 章讲**写 Lance**(thin-client),本章讲**读 Lance**(绕过 DeltaCat)。两边拼起来:用户用 Ray worker 并行写 Lance 目录→ DeltaCat 注册 manifest → 用户按需 lazy 读。DeltaCat 在 Lance 数据生命周期里只占「注册 / 版本」那一段,不碰真实数据。\n\n**下面 [#2] 先看 Lance 在内存里的身份是什么。**",
      "tags": ["导读", "role:entry", "priority:P1"],
      "color": "#f59e0b"
    },
    {
      "file": "deltacat/types/media.py",
      "line": 137,
      "location": "DatasetType",
      "title": "Gap: DatasetType.LANCE 的注释就已经预告了 gap",
      "description": "**结论:** `DatasetType.LANCE = 「lance」` 的行内注释 `# lance.LanceDataset (lazy; O(1) random access via take())` 暴露了本章 gap 的根因 —— Lance 在 DeltaCat 里被定义成**一个 lazy handle**,不是一坨 eager data,所以它天然不适合包进「读一下立刻还给你 PyArrow Table」的 `TABLE_TYPE_TO_READER_FUNC` 模型。\n\n**这一段做什么:**\n\n`DatasetType` enum(类定义在第 127 行,docstring 131–134 行)枚举**内存中**的数据表示 —— 每个 enum 值对应一种 Python 类型。调用方拿到 `DatasetType.X` 后,用它去查 `TABLE_TYPE_TO_READER_FUNC`(见 [#3])决定用哪个 reader 函数解。\n\n**关注变量:**\n\n| enum 值 | 对应 Python 类型 | 语义 |\n|---|---|---|\n| `LANCE` | `lance.LanceDataset` | **lazy handle** — 元数据在内存,数据在磁盘按需取 |\n| `PYARROW` | `pa.Table` | eager — 全部数据已在内存 |\n| `PANDAS` | `pd.DataFrame` | eager |\n| `POLARS` | `pl.DataFrame` | eager |\n| `NUMPY` | `np.ndarray` | eager |\n| `PYARROW_PARQUET` | `pa.parquet.ParquetFile` | eager |\n\n**为什么单独拎出来看:** 注意 LANCE 和其他五个**不是同一种东西**。其他的都是「把字节读进内存的 container」,LANCE 是「元数据 + 按需取数据的句柄」。你不会用同一个 reader 函数签名把这两类统一喂给 `download_manifest_entries`。\n\n**本次取值:** 第 137 行的 enum `LANCE = 「lance」`,注释写着 `# lance.LanceDataset (lazy; O(1) random access via take())`。这条注释就是本章的题眼。\n\n**下一步 [#3] 去看 `TABLE_TYPE_TO_READER_FUNC` —— 那里能直接看到 LANCE 被漏掉的事实。**",
      "tags": ["Gap", "role:main", "priority:P1"],
      "color": "#f59e0b"
    },
    {
      "file": "deltacat/types/tables.py",
      "line": 107,
      "location": "TABLE_TYPE_TO_READER_FUNC",
      "title": "Gap: TABLE_TYPE_TO_READER_FUNC 里没有 LANCE 项",
      "description": "**结论:** 这张 dispatch 表**只有 5 项,没有 LANCE**。`DatasetType.LANCE` 是被承认的合法内存类型([#2]),但没人给它注册 reader 函数 —— 这就是 Chapter 5 要讲的「gap」的代码证据。\n\n**这一段做什么:**\n\n`TABLE_TYPE_TO_READER_FUNC` 是 Chapter 4 讲过的读分发表 —— 调用 `download_manifest_entries(table_type=DatasetType.X)` 时,内部用 `TABLE_TYPE_TO_READER_FUNC[X.value](path, ...)` 找到对应的 reader 函数,把一个 manifest entry 路径读成内存数据。\n\n**关注这张表的全部 5 项:**\n\n| 行 | DatasetType | reader 函数 |\n|---|---|---|\n| 108 | `PYARROW_PARQUET` | `pa_utils.file_to_parquet` |\n| 109 | `PYARROW` | `pa_utils.file_to_table` |\n| 110 | `PANDAS` | `pd_utils.file_to_dataframe` |\n| 111 | `NUMPY` | `np_utils.file_to_ndarray` |\n| 112 | `POLARS` | `pl_utils.file_to_dataframe` |\n| — | **(缺)** `LANCE` | **(缺)** 应该是 `lance_utils.read_lance_table`([#5]) |\n\n**关注逻辑:**\n\n- 这张表只覆盖**本地数据类型**。分布式的 `DAFT` / `RAY_DATASET` / `MATERIALIZED_DATASET` 走另一条 dispatch(不在这张表里)。\n- LANCE 属于「本地」类别([#2] 里 `DatasetType.local()` 会把 LANCE 归到本地侧),所以按理也应该在这张表里。\n- `lance_utils.read_lance_table`([#5])的签名兼容 `(path, filesystem, ...)` —— 完全可以注册进来;只是目前没人连。\n\n**本次取值:**\n\n```python\nTABLE_TYPE_TO_READER_FUNC = {\n    「pyarrow_parquet」: pa_utils.file_to_parquet,\n    「pyarrow」:         pa_utils.file_to_table,\n    「pandas」:          pd_utils.file_to_dataframe,\n    「numpy」:           np_utils.file_to_ndarray,\n    「polars」:          pl_utils.file_to_dataframe,\n    # 「lance」: 缺席\n}\n```\n\n**这是为什么要走 [#4] 里的 workaround:** 既然 dispatch 表里没 LANCE,用户就没法指望 `dc.read_table(table_type=DatasetType.LANCE)` 这种调用形状能正常工作 —— 实验脚本只能自己绕开,去 catalog 拉路径再用 `lance.dataset(path)` 读。",
      "tags": ["Gap", "role:main", "priority:P0"],
      "color": "#ef4444"
    },
    {
      "file": "experiments/02_read_local.py",
      "line": 1,
      "title": "Gap: 实验脚本的 docstring 直接坦白绕开了 DeltaCat",
      "description": "**结论:** `experiments/02_read_local.py` 的前 11 行 docstring 直接写明「open-source deltacat doesn't have Lance-aware read dispatch yet」—— 这是作者亲口承认 [#3] 那个 gap,并解释绕开方法:从 catalog 拿路径,然后**直接用 `lance.dataset(path)`**。\n\n**这一段做什么:**\n\n文件级 docstring + 三个 pattern 函数的目录。读者一打开这个脚本就知道:\n\n1. 这里**不用** DeltaCat 的读 API;\n2. 只用 DeltaCat 的 catalog 来 resolve manifest entry 的路径;\n3. 读数据那一步全部交给 Lance 自己的 API。\n\n**关注 docstring 的三段:**\n\n> The open-source deltacat doesn't have Lance-aware read dispatch yet, so we resolve manifest entries (paths) from the catalog, then read the Lance datasets directly. This mirrors what deltacat-dev does internally.\n>\n> Pattern A: Eager (full PyArrow load from all Lance files)\n> Pattern B: Lazy (O(1) random access via unified Lance dataset)\n> Pattern C: Distributed (Ray workers each read a subset)\n\n**「mirrors what deltacat-dev does internally」是什么意思:** 内部版 deltacat-dev 把 Lance 接到了 dispatch 里(注册了 LANCE → `read_lance_table`),open-source 版没 backport 那一块。但即使内部版,生产代码通常也**不会**走 `dc.read_table` —— 因为 lazy 语义被 eager dispatch 漏斗吃掉了。所以这个 workaround 不仅是「没办法」,也是**正确的用法**。\n\n**本次取值(常量定义在 23–27 行,后面所有 pattern 复用):**\n\n| 常量 | 值 |\n|---|---|\n| `CATALOG_ROOT` | `/mnt/amlfs-03/shared/jingwang/WORKSPACE/PlayRayDeltaCAT/output/catalog` |\n| `DATA_DIR` | `/mnt/amlfs-03/shared/jingwang/WORKSPACE/PlayRayDeltaCAT/output/data` |\n| `CATALOG_NAME` | `「tutorial」` |\n| `NAMESPACE` | `「mecka」` |\n| `TABLE_NAME` | `「feb2_small_batch」` |\n\n**下面 [#5]–[#6] 先看 Chapter 3 已经造好的 Lance reader helper —— 它们明明存在,却没被 [#3] 引用到。**",
      "tags": ["Gap", "role:main", "priority:P0"],
      "color": "#ef4444"
    },
    {
      "file": "deltacat/utils/lance_utils.py",
      "line": 432,
      "location": "read_lance_table",
      "title": "lance_utils: read_lance_table — 造好了但没被 dispatch 引用",
      "description": "**结论:** `read_lance_table(path, filesystem, ..., include_columns, row_filter) -> pa.Table` 的签名和 `TABLE_TYPE_TO_READER_FUNC` 里其他 5 个 reader 完全兼容,可以直接注册。但它**目前没被 [#3] 那张表引用到**,所以它现在只是一个「可调用但无人调用」的孤儿函数。\n\n**这个函数做什么:**\n\n签名:`read_lance_table(path, filesystem=None, catalog_root=None, include_columns=None, row_filter=None, data_root=None) -> pa.Table`\n\n把一个 Lance 目录**全部读进一个 PyArrow Table** —— 也就是 eager 模式。如果传了 `include_columns` 就只读那几列(column projection),传了 `row_filter` 就在 Lance scanner 里做谓词下推。\n\n**关注逻辑:**\n\n1. 第 446 行:`path = _resolve_path(path, catalog_root)` —— 处理「manifest 里存的路径是相对于 catalog_root 的相对路径」这种情况。\n2. 第 447 行:`storage_options = _storage_options_from_filesystem(filesystem, data_root=data_root)` —— 把 pyarrow filesystem 对象翻译成 Lance 需要的 `storage_options` dict(S3 endpoint / credentials 等)。\n3. 第 448 行:`ds = lance.dataset(path, storage_options=...)` —— 这一行**不读数据**,只构造 lazy handle。\n4. 第 450–454 行:构造 kwargs,只在需要时加 `columns` / `filter`。\n5. 第 462 行:`return ds.to_table(**kwargs)` —— 这一行**才是真正的 I/O**,把 Lance 转成 eager PyArrow Table。\n\n**如果把它连进 dispatch 会怎样(假想):**\n\n```python\n# 这是现在 TABLE_TYPE_TO_READER_FUNC 缺的那一行\n# DatasetType.LANCE.value: lance_utils.read_lance_table,\n```\n\n连进去后,`dc.read_table(..., table_type=DatasetType.LANCE)` 就能工作,但**返回的是 eager 的 `pa.Table`**—— 这样就把 Lance 最有价值的 lazy / O(1) 语义吃掉了。这就是为什么作者宁愿让用户自己走 `lance.dataset(path)`(=[#6] `open_lance_dataset`)。\n\n**本次取值:** 在本章里,`read_lance_table` **不会**被 02_read_local.py 调用 —— 那个脚本的 Pattern A([#7])直接写 `lance.dataset(path).to_table()`,和这个函数里第 448 + 462 行的代码一模一样,但少了 `_resolve_path` 和 `storage_options` 的包装。",
      "tags": ["lance_utils", "role:main", "priority:P1"],
      "color": "#f59e0b"
    },
    {
      "file": "deltacat/utils/lance_utils.py",
      "line": 465,
      "location": "open_lance_dataset",
      "title": "lance_utils: open_lance_dataset — lazy handle,Pattern B 的 API 形状",
      "description": "**结论:** `open_lance_dataset(path, ...) -> lance.LanceDataset` 返回一个 **不加载数据的 lazy handle**,支持 `ds.take(indices)`(O(1) 随机访问)、`ds.count_rows()`(从 manifest 读)、`ds.to_table(columns=...)`(按需读某几列)。这才是符合 [#2] `DatasetType.LANCE` 注释所说的「lazy; O(1) random access」的 API 形状。\n\n**这个函数做什么:**\n\n签名:`open_lance_dataset(path, filesystem=None, catalog_root=None, data_root=None) -> lance.LanceDataset`\n\n只做两件事:解析路径 + 打开 Lance dataset。**不读任何数据**。返回的 handle 暴露了 Lance 的三个核心 API,正好映射到 Chapter 5 的 Pattern B([#8])三种调用形状:\n\n| API | 本次用法 | 作用 | 成本 |\n|---|---|---|---|\n| `ds.take([0, 100, 200])` | [#8] 第 87 行 | O(1) 随机取 3 行 | ~3 × fragment 打开成本 |\n| `ds.count_rows()` | [#8] 第 83 行 | 读行数 | 0 I/O(从 manifest 取) |\n| `ds.to_table(columns=[...])` | [#8] 第 94 行 | 只读某几列,跳过 video 大 blob | I/O 与选中列成正比 |\n\n**关注逻辑:**\n\n1. 第 478 行:`_resolve_path` —— 同 [#5]。\n2. 第 479 行:`_storage_options_from_filesystem` —— 同 [#5]。\n3. 第 480 行:`return lance.dataset(path, storage_options=...)` —— 只构造 handle,**立即返回**。\n\n**和 [#5] 的差别:** `read_lance_table` 在第 462 行多调了一次 `ds.to_table(**kwargs)`,把 lazy 立即变 eager;`open_lance_dataset` 到 480 行就返回,把「什么时候读 / 读多少」的决定权交给调用方。\n\n**为什么 dispatch 表里不适合放它:** `TABLE_TYPE_TO_READER_FUNC` 的契约是「给 path 返回内存对象」,这个函数虽然也满足签名,但 **返回物是 lazy handle** —— 下游 `download_manifest_entries` 假设返回物是 eager data 然后 `pa.concat_tables(...)`,把一堆 lazy handle concat 起来语义根本不成立。所以即便有这个函数,也没法直接塞进 dispatch。这就是 gap 存在的**技术根因**。\n\n**下一步 [#7]–[#10] 看 `experiments/02_read_local.py` 三种 pattern 如何拿着这个 lazy handle(或不拿)干三种事。**",
      "tags": ["lance_utils", "role:main", "priority:P1"],
      "color": "#f59e0b"
    },
    {
      "file": "experiments/02_read_local.py",
      "line": 30,
      "location": "get_lance_paths",
      "title": "Pattern 准备: get_lance_paths — 拿到 344 个 Lance 目录的绝对路径",
      "description": "**结论:** 三种 pattern 都从这一步开始 —— 扫 `DATA_DIR` 拿到所有 `.lance` 结尾的目录,排序后返回一个 list。本次运行返回 344 个路径。\n\n**这个函数做什么:**\n\n签名:`get_lance_paths() -> List[str]`\n\n对 `DATA_DIR`(`/mnt/amlfs-03/shared/.../output/data`)做 `os.listdir`,筛出 `.lance` 结尾的名字,`os.path.join` 加上根路径,最后 `sorted(...)` 确保跨 pattern 读到的顺序一致(方便对照)。\n\n**为什么用 listdir 而不是走 catalog:** 02_read_local.py 的 docstring([#4])说了 —— 跳过 DeltaCat 的读路径。严格起见,生产代码应该:\n\n1. `dc.read_table(...)` 拿到 `Manifest` 对象(包含所有 ManifestEntry);\n2. 遍历每个 entry 的 `url` 字段,解析成绝对路径;\n3. 再用这些路径去 `lance.dataset(path)`。\n\n但在这个教学脚本里为了简洁,直接 listdir。两种方式拿到的路径**集合完全一样** —— 因为 `build_manifest_entries`(第 3 章 [#5])就是按目录建 entry 的。\n\n**本次取值:**\n\n- `DATA_DIR`:`/mnt/amlfs-03/shared/jingwang/WORKSPACE/PlayRayDeltaCAT/output/data`\n- 返回 list 长度:**344**(因为 feb2_small_batch 有 344 个 episode)\n- 每个元素形如:`/mnt/amlfs-03/.../data/episode_XYZ.lance`(一个 Lance 目录)\n- 总大小:**7.15 GB**(视频 blob 占大头,每 episode ~20 MB)\n\n**下面 [#8]–[#10] 三个 pattern 函数都以这个 list 作为起点。**",
      "tags": ["Pattern 准备", "role:utility-dive", "priority:P3"],
      "color": "#9ca3af"
    },
    {
      "file": "experiments/02_read_local.py",
      "line": 39,
      "location": "pattern_a_eager",
      "title": "Pattern A: eager — 全部读进单个 PyArrow Table",
      "description": "**结论:** 最朴素的读法 —— 循环 344 次 `lance.dataset(path).to_table()`,最后 `pa.concat_tables(...)` 拼起来。本地 **24.1s / 297 MB/s**,S3 **33.3s**(见 experiment.md 2.3)。适合小数据 + 需要一次性全部塞进内存的场景。\n\n**这个函数做什么:**\n\n签名:`pattern_a_eager() -> pa.Table`\n\n三阶段:\n\n1. **第 42 行** `lance_paths = get_lance_paths()` — 拿 344 个目录。\n2. **第 45–49 行 的循环** — 对每个目录 `lance.dataset(path).to_table()`,放进 `tables` list;循环结束 `pa.concat_tables(tables)` 合成一张大表。\n3. **第 55–56 行** — 打印指标:`num_rows / num_columns / nbytes / elapsed`。\n\n**关注逻辑:**\n\n- `lance.dataset(path)` **不读数据**(只建 handle),`.to_table()` **才读数据**(一次性全加载)。\n- `pa.concat_tables` 要求 **所有表 schema 一致** —— 在 feb2 场景 schema 由 `write_lance_table`(Chapter 3)统一写入,天然一致。\n- 返回的 `result` 是**一整张** PyArrow Table(344 行 × N 列),**整个在内存里**。如果数据集是 35 GB 的 feb_08,这一行会 OOM。\n\n**关注变量:**\n\n| 变量 | 首次出现 | 作用 |\n|---|---|---|\n| `lance_paths` | 第 42 行 | 344 个 Lance 目录路径 |\n| `tables` | 第 45 行 | 344 个 PyArrow Table 的暂存 list |\n| `result` | 第 49 行 | 最终合并后的 PyArrow Table |\n| `elapsed` | 第 50 行 | perf_counter 计时 |\n\n**本次取值(experiment.md 2.3 节):**\n\n| 环境 | Time | 吞吐 |\n|---|---|---|\n| 本地 FS | **24.1s** | ~297 MB/s |\n| S3 (compacted 单文件) | **33.3s** | — |\n| S3 (uncompacted 344 文件) | **473s**(!) | **差 338×** —— 见 experiment.md 2.4 节 |\n\n**什么时候该用 Pattern A:** 小数据(能整装内存)、要做 OLAP 扫描(需要全部列、全部行)、一次性计算。**不适合**:训练 dataloader、按索引采样、大数据集。那些场景请看 [#9] / [#10]。",
      "tags": ["Pattern A", "role:main", "priority:P2"],
      "color": "#3b82f6"
    },
    {
      "file": "experiments/02_read_local.py",
      "line": 63,
      "location": "pattern_b_lazy",
      "title": "Pattern B: lazy — Lance 的核心卖点:O(1) take + 列投影",
      "description": "**结论:** 这是 Lance **最值得学的 pattern**。先把 344 个小 Lance dataset merge 成一个大 dataset(模拟 compaction),然后用 `ds.take([0,100,200])` 做 **0.34s 本地 / 7.9s S3** 的 O(1) 随机取样,用 `ds.to_table(columns=[...])` 做 **0.007s 本地 / 1.6s S3** 的列投影。训练 dataloader 的理想调用形状。\n\n**这个函数做什么:**\n\n三阶段:\n\n1. **Merge(第 73–80 行)** — 把 344 个小 Lance 目录合并成一个 `_merged.lance`。生产里由 DeltaCat compaction 做(Chapter 2),这里为了教学 ad-hoc 现做。如果 `_merged.lance` 已存在就跳过。\n2. **count_rows(第 82–83 行)** — `ds.count_rows()` 从 manifest 读,0 I/O。\n3. **take(第 86–90 行)** — `ds.take([0, 100, 200])` **O(1)** 随机取 3 行。Lance 把 row number 映射到 `(fragment_id, offset)`,只读那三个 fragment 的那三个 offset。\n4. **列投影(第 92–96 行)** — `ds.to_table(columns=[「episode_id」, 「duration」, 「environment_id」])` 只读 3 个 metadata 列,**完全跳过** video_bytes 大 blob。\n\n**为什么 O(1) 成立:**\n\nLance 的 fragment 粒度来自 Chapter 3 `write_lance_table` 的 `max_rows_per_fragment = 100_000`。这个小粒度让 row-number → fragment 的映射表常驻内存;`take([i])` 的成本 ≈ 一次 fragment 文件打开 + 一次 row decode。**与总行数无关 = O(1)**。\n\n对比 Parquet:Parquet 默认 row group 1M 行,读一行要解压整个 row group ≈ O(row_group_size)。所以同样的视频训练采样场景,Lance 比 Parquet 快**几个量级**。\n\n**关注变量:**\n\n| 变量 | 首次出现 | 作用 |\n|---|---|---|\n| `merged_path` | 第 73 行 | 合并后的单 Lance 目录路径 |\n| `ds` | 第 82 行 | 合并 dataset 的 **lazy handle**(同 [#6] 返回物) |\n| `sample` | 第 87 行 | take 的结果,只含那 3 行全部列 |\n| `meta_only` | 第 94 行 | 列投影结果,所有行 × 3 列 |\n| `t_take` / `t_proj` | 第 88/95 行 | 分别计时 |\n\n**本次取值(experiment.md 2.3 节):**\n\n| 操作 | 本地 | S3 |\n|---|---|---|\n| `ds.take([0,100,200])`(3 行全列,含 video) | **0.34s** | 7.9s |\n| `ds.to_table(columns=[...3 metadata cols...])` | **0.007s** | 1.6s |\n\n`0.007s` 读 344 行 metadata 本地 —— 比 Pattern A 的 24.1s 快 **3400 倍** —— 因为完全跳过了 7 GB 的 video blob。\n\n**什么时候该用 Pattern B:** 训练 dataloader、推理按 episode ID 采样、metadata-only 查询(生成 index)、任何你**不需要所有列所有行**的场景。这就是 [#2] `DatasetType.LANCE` 的注释所说的 lazy 语义的现实价值。",
      "tags": ["Pattern B", "role:main", "priority:P0"],
      "color": "#ef4444"
    },
    {
      "file": "experiments/02_read_local.py",
      "line": 100,
      "location": "pattern_c_distributed",
      "title": "Pattern C: distributed — Ray worker 分片并行读",
      "description": "**结论:** Pattern A / B 都是单机读,数据一旦到 35 GB 量级就 OOM / 慢到不可用。Pattern C 用 Ray 把 344 个路径切块,每个 worker 处理一个 chunk,**并行读**。本 demo 用 10 个 worker。生产规模的相同形状在 [#11]–[#14] 里拿 35 GB 数据集去打实测。\n\n**这个函数做什么:**\n\n四阶段:\n\n1. **定义 `worker_read`(第 104–115 行)** — `@ray.remote` 函数,每个 worker 拿一个路径子集,循环读、`to_table(columns=[...])` 做列投影、`pa.concat_tables` 局部合并,返回 `{num_rows, episode_ids}`(**注意不返回大表**,避免序列化成本)。\n2. **`ray.init`(第 117 行)** — 连到已经启动的 Ray cluster(`address=「auto」` = 连本机或环境变量指向的 head)。\n3. **切 chunk(第 119–121 行)** — `chunk_size = len(lance_paths) // num_workers + 1 = 344 // 10 + 1 = 35`,所以前 9 个 worker 各拿 35 条,第 10 个拿剩下的。\n4. **submit + collect(第 123–131 行)** — `worker_read.remote(chunk)` 提交,`ray.get(futures)` 等全部完成,统计总行数。\n\n**关注逻辑:**\n\n- **为什么 worker 内部做列投影:** 如果每个 worker 把全部列(包括 video)读回来再 concat,Ray 对象存储会爆。只读 3 个 metadata 列,每 worker 返回的 dict 很小。\n- **为什么用 `i % num_workers`:** 这里其实用的是连续切片 `lance_paths[i * chunk_size:(i + 1) * chunk_size]`,不是 round-robin。连续切片在路径**顺序无关**时 OK;但如果 Lance 目录按 size 排序,连续切片会导致负载不均。`experiments/10_read_scaling_s3.py` 用的是 `assignments[i % n_workers].append(path)`(见 [#11]),是 round-robin,更均匀。\n\n**关注变量:**\n\n| 变量 | 首次出现 | 作用 |\n|---|---|---|\n| `num_workers` | 第 120 行 | = 10 |\n| `chunk_size` | 第 121 行 | = 35(344 / 10 + 1) |\n| `futures` | 第 124 行 | 每个 worker 一个 `ObjectRef` |\n| `results` | 第 130 行 | 每个元素是 `{num_rows, episode_ids}` dict |\n\n**本次取值:**\n\n- `num_workers = 10`,`chunk_size = 35`;\n- 每个 worker 只读 3 个 metadata 列(跳过 video blob);\n- 返回的不是大 PyArrow Table 而是小 dict,避免跨进程序列化。\n\n**下一步 [#11]–[#14] 把这个 pattern 放大到 35 GB / 54M 行,去找「shard 数 × worker 数」的最优点。**",
      "tags": ["Pattern C", "role:main", "priority:P2"],
      "color": "#3b82f6"
    },
    {
      "file": "experiments/10_read_scaling_s3.py",
      "line": 25,
      "location": "read_worker_full",
      "title": "Shard 实测: read_worker_full — 生产规模的 Ray worker",
      "description": "**结论:** `read_worker_full` 是 [#10] Pattern C 的生产级版本。一个 `@ray.remote(num_cpus=1)` 函数,每个 worker 拿一批 shard path,循环 `lance.dataset(path, storage_options=S3) -> to_table()` **读全部列**,返回 `(total_rows, total_bytes)`。用 S3 `storage_options` 把 endpoint / credentials 传给 Lance。\n\n**这个函数做什么:**\n\n签名:`read_worker_full(shard_paths: List[str], storage_options: Dict) -> Tuple[int, int]`\n\n做三件事:\n\n1. **第 29–30 行** — 对每个 shard path `lance.dataset(path, storage_options=...)` 建 handle;\n2. **第 31 行** — `ds.to_table()` **读全部列**(含 video 大 blob);\n3. **第 32–33 行** — 累计 `total_rows` 和 `total_bytes`;\n4. **第 34 行** — 返回 `(total_rows, total_bytes)`,**不**返回大 PyArrow Table,避免 Ray object store 压力。\n\n**为什么返回 `total_bytes` 而不是 Table:** 同 [#10] 里讲的 —— Ray object store 的跨节点传输是 zero-copy 但仍然有序列化 / 反序列化 overhead,35 GB 往 head node 返会炸。这里只返回 `(int, int)`,ray driver 聚合出总吞吐。\n\n**STORAGE_OPTIONS(14–20 行):**\n\n```python\n{\n    「aws_access_key_id」:     env,\n    「aws_secret_access_key」: env,\n    「aws_endpoint」:          「https://pdx.s8k.io」,  # SwiftStack\n    「aws_region」:            「us-east-1」,\n    「allow_http」:            「false」,\n}\n```\n\n这是 Lance **直接**用的 endpoint 形式(不经过 DeltaCat 的 filesystem 抽象)—— 因为 DeltaCat 没管 Lance 读路径,[#5]`read_lance_table` 的 `_storage_options_from_filesystem(...)` 那层也就绕过去了。\n\n**还有一个兄弟函数 read_worker_meta(37–44 行):** 和 full 版一样,但 `ds.to_table(columns=columns)` 传入 `META_COLUMNS = [「index」, 「episode_index」, 「frame_index」, 「timestamp」, 「task_index」, 「subtask_index」]`,只读 6 列。对应 experiment.md 3.2 节的 metadata-only 实测(读 35GB 数据只用 6.7–13.5s)。\n\n**下面 [#12] 看 `run_scaling` 驱动这个 worker,构造 shard × worker 矩阵。**",
      "tags": ["Shard 实测", "role:main", "priority:P1"],
      "color": "#f59e0b"
    },
    {
      "file": "experiments/10_read_scaling_s3.py",
      "line": 47,
      "location": "run_scaling",
      "title": "Shard 实测: run_scaling — 驱动 shard × worker 矩阵",
      "description": "**结论:** `run_scaling(label, worker_fn, shard_counts, worker_counts)` 是扫矩阵的驱动器。对 `shard_counts × worker_counts` 的每个组合,构造 shard 路径、round-robin 分给 workers、submit 到 Ray、计时。本次 feb_08 用 `shard_counts=[1,5,10,20,50,100]` × `worker_counts=[1,2,5,10,20,50,100]` = **42 个组合**。结果就是 experiment.md 3.1 那张表。\n\n**这个函数做什么:**\n\n签名:`run_scaling(label, worker_fn, shard_counts, worker_counts, extra_args=()) -> None`\n\n外层循环 shard_counts,内层循环 worker_counts,对每个 (n_shards, n_workers):\n\n1. **第 61–62 行** — shard dir = `s3://GearHome/.../shards/n{n_shards:03d}/`,生成 shard_0000.lance..shard_{n-1}.lance 路径 list。\n2. **第 66–67 行** — 跳过 `n_workers > n_shards * 5` 的组合(worker 多过 shard × 5 没意义)。\n3. **第 69–72 行** — round-robin 分配:`assignments[i % n_workers].append(path)`,保证均匀。\n4. **第 74–77 行** — 提交所有 worker 任务、`ray.get` 等全部完成、计时。\n5. **第 86–88 行** — 第一个成功的配置设为 baseline,后续计算 speedup。\n\n**为什么 round-robin 而不是连续切片:** 见 [#10] 对照。连续切片在 shard size 差异大时负载不均;round-robin 保证每个 worker 的字节数差不多。\n\n**本次 feb_08 的跑法(main 函数 101–111 行):**\n\n```python\nshard_counts = [1, 5, 10, 20, 50, 100]   # 总 bytes 固定 35GB,切几份\nworker_counts = [1, 2, 5, 10, 20, 50, 100]\n# full read(全部列,含 video): 42 个组合\nrun_scaling(「FULL READ SCALING」, read_worker_full, shard_counts, worker_counts)\n```\n\n**跑出来的 experiment.md 3.1 表(部分摘录):**\n\n| Shards \\ Workers | 1 | 2 | 5 | 10 | 20 | 50 | 100 |\n|---|---|---|---|---|---|---|---|\n| 1 (35 GB) | 194s (1x) | 163s | 173s | — | — | — | — |\n| **5 (7 GB)** | 108s | 54s | **19s (10.2x)** ⭐ | 23s | 30s | — | — |\n| 10 (3.5 GB) | 135s | 56s | 24s | 21s | 22s | 15s (12.8x) | — |\n| 50 (700 MB) | 534s | 254s | 109s | 53s | 33s | 23s | 18s |\n| 100 (350 MB) | **797s** ⚠️ | 412s | 182s | 98s | 53s | 26s | 18s |\n\n**两个刺眼的数字:**\n\n- ⭐ **5 shards + 5 workers = 19s** —— 最优性价比。\n- ⚠️ **100 shards + 1 worker = 797s** —— 是 1 shard + 1 worker(194s)的 **4 倍慢**。原因:每 shard 固定开销 ~3–8s(S3 连接 + manifest 读 + fragment 元数据解析),100 个 shard 就是 300–800s。这就是「shard 太多」的惩罚。\n\n**下一步 [#13] 看第三个数据集 mar_6_umi 怎么印证最优点在同一区间,[#14] 汇总三个数据集的跨比较。**",
      "tags": ["Shard 实测", "role:main", "priority:P0"],
      "color": "#ef4444"
    },
    {
      "file": "experiment.md",
      "line": 129,
      "title": "Shard 实测: mar_6_umi 印证最优点在同一区间",
      "description": "**结论:** 第三个数据集 `mar_6_umi_lerobot`(18.5 GB, 54M 行, 340B/行)独立地给出**同一结论**:最优点在 **5 shards + 10–20 workers**,最快 **4.6–4.8s / 15–16× speedup**,峰值吞吐 **3.9 GB/s** —— 三个数据集都指向「shards ≈ workers,不是 `ceil(bytes/512MB)`」。\n\n**关注 experiment.md 4.1 节的这张矩阵(18.5 GB full read):**\n\n| Shards \\ Workers | 1 | 2 | 5 | 10 | 20 | 50 | 100 |\n|---|---|---|---|---|---|---|---|\n| 1 (18.5 GB) | 74s (1x) | 48s | 50s | — | — | — | — |\n| **5 (3.7 GB)** | 38s | 15s | 7.5s | **4.8s (15.6x)** ⭐ | 4.6s (16.1x) | — | — |\n| 10 (1.85 GB) | 47s | 16s | 7.4s | 6.9s | 6.1s | 5.3s | — |\n| 20 (925 MB) | 81s | 37s | 16s | 10s | 14s | 7.3s | 8.9s |\n| 50 (370 MB) | 172s | 83s | 34s | 14s | 14s | 14s | 5.4s |\n| 100 (185 MB) | 284s | 89s | 36s | 18s | 10s | 7.8s | 6.0s |\n\n**对比 feb_08 [#12] 的观察:**\n\n| 维度 | feb_08(35 GB, 650B/行) | mar_6(18.5 GB, 340B/行) |\n|---|---|---|\n| 最优配置 | 5 shards + 5 workers | 5 shards + 10–20 workers |\n| 最快时间 | 19s (10.2x) | 4.6s (15.6x) |\n| 峰值吞吐 | 2.3 GB/s | **3.9 GB/s** |\n| Shard 过少(1 shard)最差时间 | 194s | 74s |\n| Shard 过多(100 shard)且 worker=1 | 797s | 284s |\n\n**两个数据集都印证:**\n\n1. **最优 shard 数是 5** —— 不是按字节算的 70(35GB / 512MB)或 36(18.5GB / 512MB);\n2. **Worker 数可以独立扩张到 10–20** —— 但再扩到 50/100 收益锐减(撞到 S3 聚合带宽上限)。\n3. **Shard 100 + worker 少的组合是灾难** —— 每 shard 固定开销累积(manifest 读 + fragment 元数据解析,~3–8s per shard)。\n\n**下一步 [#14] 汇总三个数据集的跨比较,引出修正公式。**",
      "tags": ["Shard 实测", "role:main", "priority:P1"],
      "color": "#f59e0b"
    },
    {
      "file": "experiment.md",
      "line": 151,
      "title": "Shard 实测: 三数据集跨比较 + 对笔记公式的修正",
      "description": "**结论:** 三个数据集的最优点集中在 `5 shards + 5–20 workers`,与数据总字节**不**成正比。DeltaCat 原笔记的 `Hash Buckets = ceil(bytes / 512 MB)` 公式会给出 **70 个 shards** (35GB) 或 **40K shards** (20TB),远超训练 worker 数,徒增 S3 per-object 开销。正确公式应是 `Compacted_files = max(min_shards, num_training_workers)`。\n\n**experiment.md 4.3 节的跨数据集对比表:**\n\n| 指标 | feb2_small | feb_08_500hr | mar_6_umi |\n|---|---|---|---|\n| 数据量 | 7.15 GB | 35 GB | 18.5 GB |\n| 行数 | 344 | 54M | 54M |\n| **行大小** | **~20 MB**(含 video) | ~650 B | ~340 B |\n| 最优 shards × workers | 20 + 50 | **5 + 5** | 5 + 10 |\n| Speedup | 2.4x | **10.2x** | **15.6x** |\n| 最快时间 | 14.6s | 19s | 4.6s |\n| 峰值吞吐 | 488 MB/s | 2.3 GB/s | **3.9 GB/s** |\n\n**两条核心结论(4.3 节的「关键发现」):**\n\n1. **行越小,S3 吞吐越高** — mar_6 (340B/行) → 3.9 GB/s;feb_08 (650B/行) → 2.3 GB/s;feb2 (20MB/行,含 video blob) → 仅 488 MB/s。大 blob 让 per-GET 开销摊不薄。\n2. **最优点聚集在 `5 shards + 5–20 workers`** — 三个数据集都在这区间达到最优。说明「最优 shard 数」**不是**总字节的函数,而是 **reader 并行度**的函数。\n\n**experiment.md 5.3 节对原笔记公式的修正:**\n\n| 策略 | 35GB 实测 | 推论(20TB) |\n|---|---|---|\n| 笔记原公式:`ceil(bytes/512MB)` | 70 shards;单 worker 534s,50 workers 23s | 40K shards → 需 40K workers 才能跟上 |\n| **最优:`shards ≈ workers`** | **5 shards + 5 workers = 19s** | **100 shards + 100 workers** |\n| 灾难:`shard >> workers` | 100 shards + 5 workers = 182s(9.6x 慢) | 严重浪费 |\n\n**修正后的速查公式:**\n\n```\nCompacted_files = max(min_shards, num_training_workers)\nShard_size     = total_bytes / Compacted_files\n```\n\n其中 `min_shards` 通常 5–8(避免单 shard 成为 I/O 瓶颈),`num_training_workers` 就是你训练时有几个 dataloader worker。\n\n**为什么笔记原公式**可能**在其原始场景成立(experiment.md 5.4 节):** 原公式针对的可能是 **compaction 内部的 hash bucket 数**(`Phase 1 按 hash(PK) mod N 分桶,每桶独立 merge,N 要够大避免单桶 OOM`),而**不是**最终输出文件数。最终输出文件数可以在 Phase 2 merge 后进一步合并(`MAX_RECORDS_PER_COMPACTED_FILE = 4M`),使 shard 数匹配训练并行度。\n\n**下一步 [#15] 收束 —— 把三种 pattern + shard 公式打包成一张决策图。**",
      "tags": ["Shard 实测", "role:main", "priority:P0"],
      "color": "#ef4444"
    },
    {
      "title": "收束: 决策树 + shard 速查 + 与前后章的钩子",
      "description": "**核心结论:** 读 Lance 时先按「是否随机 / 是否分布式」选 pattern,再按「训练 worker 数」设 shard;**不要**按总字节算 shard 数。\n\n**读 pattern 决策树:**\n\n```\n                    需要读 Lance 数据?\n                          │\n         ┌────────────────┼────────────────┐\n         │                │                │\n   全部塞进内存?     按索引随机采样?   超大数据集 + Ray?\n   单机 OLAP?        训练 dataloader?  生产规模?\n         │                │                │\n         ▼                ▼                ▼\n   Pattern A         Pattern B          Pattern C\n   lance.dataset(p)  lance.dataset(p)   @ray.remote\n     .to_table()       .take([...])     read_worker_full\n   (pa.concat)         .to_table(       (round-robin\n                        columns=[...])    分 shard)\n         │                │                │\n   本地 24.1s [#8]    本地 0.34s [#9]   见 [#11]–[#14]\n   S3 33.3s           S3 7.9s           shard × worker 矩阵\n```\n\n**Shard 数速查:**\n\n| 场景 | shard 数 | 出处 |\n|---|---|---|\n| 训练 N 个 dataloader worker | `max(5, N)` | [#14] 修正公式 |\n| 小数据集(<1 GB) | 1 | [#11] feb2 矩阵 |\n| 中数据集 10–50 GB | 5–20 | [#12] + [#13] |\n| **不要**用 `ceil(bytes/512MB)` | ❌ | [#14] —— 那是内部 hash bucket,不是输出文件 |\n\n**S3 吞吐的**硬**上限(experiment.md 4.3):**\n\n| 行大小 | 峰值吞吐 | 代表数据集 |\n|---|---|---|\n| 20 MB/行(视频 blob) | **488 MB/s** | feb2_small |\n| 650 B/行 | 2.3 GB/s | feb_08_500hr |\n| 340 B/行 | **3.9 GB/s** | mar_6_umi |\n\n行越大,per-GET overhead 越摊不薄。**Lance 的行大小直接决定你能榨出多少 S3 带宽。**\n\n**与前后章的钩子:**\n\n- **Chapter 3 的闭环:** 第 3 章讲 `write_lance_table` 默认 `max_rows_per_fragment=100_000`,本章 [#9] 的 `take([i])` O(1) 就是吃那个小 fragment 粒度的红利。写路径的设计决定读路径能玩多快 —— 两章合起来讲通了 Lance 为什么在 DeltaCat 里值得单开一条 thin-client 分支。\n- **Chapter 4 的对照:** 第 4 章讲完 `TABLE_TYPE_TO_READER_FUNC` 的标准 dispatch 后,你可能会问「Lance 为什么不在表里」。答案就是本章 [#2]–[#4] 的三步证明 —— 不是漏了,是 lazy 语义塞不进 eager dispatch 的漏斗。\n- **下一章方向:** 如果读者想看 DeltaCat 自己的读路径怎么在 Ray cluster 上调度,请跟 `download_manifest_entries` 内部的 parallelism 策略;如果读者想看 compaction 怎么**写出**最优 shard 数,请跟 Chapter 2 的 `compact_partition`。\n\n**读完本章后,你有能力回答:**\n\n1. 同事问:「为什么我 `dc.read_table(table_type=DatasetType.LANCE)` 报 KeyError?」→ 指 [#3] 的缺失行。\n2. 老板问:「我们 20TB 的 Lance 数据集要切几个 shard?」→ 不是 40K,是 `max(min_shards, num_training_workers)`([#14])。\n3. 自己调优:「训练 dataloader 慢,该走 Pattern A 还是 B?」→ 随机采样一律 B,`ds.take([...]) + columns=[...]`([#9])。",
      "tags": ["收束", "role:return", "priority:P1"],
      "color": "#f59e0b"
    }
  ]
}
