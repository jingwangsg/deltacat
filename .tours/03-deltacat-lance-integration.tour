{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "3 - DeltaCat Lance 集成:从 fat-client 到 thin-client",
  "description": "Commits 82cea51 + d6a4008 — Lance 格式集成与 manifest-only 写入路径",
  "overview": "# Tour · DeltaCat Lance 集成\n\n本章讲**为什么** DeltaCat 在 commit `82cea51` 里为 Lance 格式开了一条「绕过数据处理」的新写入路径,以及这条路径在代码里是怎么体现的。\n\n配套的具体数字、函数签名、调用参数都在各个 step 的 description 里;这里只讲 **大局观**。\n\n---\n\n## 1. 问题:fat-client 对 Lance 很吃亏\n\n在这两个 commit 之前,DeltaCat 写数据只有一条路径,我们称之为 **fat-client**:\n\n```\n用户  ──data──▶  dc.write_to_table\n                    │\n                    ├─ 推 schema (infer_table_schema)\n                    ├─ 切 slice\n                    ├─ 序列化成 Parquet\n                    ├─ 写文件\n                    └─ 建 manifest ──▶ commit\n```\n\n对于小到中等规模的 Parquet 工作负载,这很好。但对 Lance + 多模态视频数据来说,有三个痛点:\n\n1. **格式错位**:用户本来就想把数据存成 Lance(O(1) 随机访问 + 向量检索),结果 DeltaCat 要先把它当 Parquet 写一遍,再由用户转成 Lance?完全多余。\n2. **集群利用率低**:用户手里有 2800 CPU 的 Ray 集群,但 fat-client 模式下所有数据都汇到 DeltaCat 的单个调用点。344 个 episode 串起来要 38.8 秒,而每个 Ray worker 本可以独立写自己的那一份。\n3. **Lance 是目录不是文件**:Lance dataset 在磁盘上是一个目录(含 `_versions/`、`data/` 等子目录),不是单文件。DeltaCat 如果要自己序列化出 Lance,就要把 Lance 内部 layout 逻辑镜像一遍——既脆弱又没必要。\n\n## 2. 解法:thin-client 模式\n\n`82cea51` 的核心想法是:**让用户自己写 Lance 目录,DeltaCat 只负责把这些已经写好的目录注册进 catalog**。\n\n```\n每个 Ray worker ──lance.write_dataset──▶  episodeXYZ.lance/ (一个目录)\n                                                 │\n                                                 ▼\n                                     build_manifest_entries()\n                                                 │\n                                                 ▼\n用户 ──manifest=Manifest.of(entries=...)──▶ dc.write_to_table(data=None, manifest=..., content_type=LANCE)\n                                                 │\n                                                 ├─ 【跳过】schema 推断(没数据可推)\n                                                 ├─ 【跳过】schema evolution\n                                                 ├─ 【跳过】Parquet 序列化\n                                                 ├─ 【跳过】compaction 触发\n                                                 └─ stage_delta_from_manifest ──▶ commit_delta\n```\n\n这条路径从 [#9]`_create_table_for_write` 的分支跳过 schema 推断,到 [#13]`if manifest is not None` 的短路返回,到 [#12]`stage_delta_from_manifest` 的「只包一层 Delta」,都是在 **尽可能少做事**。\n\n## 3. Benchmark 验证(来自 `experiment.md`)\n\n| 模式 | 时间 | 吞吐 | Speedup |\n|---|---|---|---|\n| **Thin-Client (344 Ray tasks)** | **12.5s** | **572 MB/s** | **3.1x** |\n| Direct `lance.write_dataset` (单进程) | 38.8s | 184 MB/s | 1.0x |\n\nWorker scaling(本地文件系统,feb2_small_batch 344 episodes / 7.15 GB):\n\n| Workers | Time | MB/s | Speedup |\n|---|---|---|---|\n| 1 | 39.5s | 181 | 1.00x |\n| 2 | 18.1s | 395 | 2.18x |\n| 5 | 7.4s | 963 | 5.32x |\n| 10 | 3.8s | 1,879 | 10.38x |\n| **20** | **2.1s** | **3,423** | **18.91x** |\n| 50 | 1.8s | 4,054 | 22.40x |\n\n线性加速到 20 workers,之后因 344 个 episode 分不够而收敛。这条曲线是 thin-client 值回票价的证据。\n\n## 4. 三条路径的对比\n\n| 阶段 | 标准路径 (data 非空) | Manifest 路径 (manifest 非空) | 差异 |\n|---|---|---|---|\n| schema 推断 | `infer_table_schema(data)` | 跳过,从 manifest 里取 schema_id | 节省 O(rows) 遍历 |\n| 数据写入 | `_write_table` 切 slice + 序列化 | **不发生** | 节省 O(bytes) I/O |\n| content_type | 默认 PARQUET | `ContentType.LANCE` | Lance 走外置写 |\n| Schema evolution | 走 | 跳过 | manifest 里已经有 schema_id |\n| Compaction 触发 | 走 | 跳过 | 用户自己决定 |\n| stage_delta | 用 `stage_delta(data, ...)` | 用 `stage_delta_from_manifest(manifest, ...)` [#12] | 前者包含 `_write_table`,后者只包 `Delta.of(...)` |\n\n## 5. 建议阅读顺序\n\n- **从 [#1] 开始** —— 读动机,明白为什么要有这条新路径。\n- **[#2]–[#3]**:类型枚举。一共才 5 行,但这两个 enum 代表了 Lance 在 DeltaCat 里被承认了身份。\n- **[#4]–[#8]**:`lance_utils.py` 里的公共 helper。重点是 [#5]`build_manifest_entries` —— 它是 thin-client 模式的桥梁。\n- **[#11]–[#12]**:storage 层的 `stage_delta_from_manifest` —— commit 的核心。和旧的 `stage_delta` 对照着看。\n- **[#13]–[#15]**:catalog 层。签名改了,`_create_table_for_write` 多了一个分支,`write_to_table` 里多了一段短路返回。\n- **[#16]–[#17]**:用户侧的 `experiments/01_write_local.py`,看看这些 API 是怎么被调起来的。\n- **[#18]**:Ray 2.54 兼容性修复,顺手补上的。\n\n## 6. 常见困惑 & 对应排查点\n\n| 现象 | 根因 | 跳到 |\n|---|---|---|\n| 为什么 `write_to_table(data=None)` 不报错? | 签名改了,`data` 变成 keyword-only 且默认 `None` | [#13] |\n| schema 从哪来(`data is None`)? | 从 manifest 每个 entry 的 `meta.schema_id` 取 | [#5], [#15] |\n| Lance 是文件还是目录? | 目录。一个目录 = 一个 ManifestEntry | [#5] |\n| 为什么要 buffer alignment? | arrow-rs 的 FFI `ScalarBuffer<T>` 对齐要求,Lance 3.0 没有自动处理 | [#8] |\n| 为什么 `max_rows_per_fragment=100_000` 而不是 Lance 的默认 1M? | 多模态数据每行 5 KB–500 KB,1M 会超过 compactor 的内存预算 | [#7] |\n| 为什么不直接调 `lance.write_dataset` 就行? | 可以,但这样就没有 DeltaCat 的版本管理、sort scheme、compaction | [#16] |\n| Ray 2.54 起跑不起来了? | `FastFileMetadataProvider` / `ParquetMetadataProvider` 被移除了 | [#18] |\n\n---\n\n**读完本章,你会知道:**\n\n- DeltaCat 为什么在写路径上开了 thin-client 分支(答案:Lance + Ray 并行 + 多模态数据体积大);\n- 这条分支在代码里体现在哪(答案:[#12] `stage_delta_from_manifest` + [#13] `if manifest is not None` 两处短路);\n- 用户要写 Lance 数据时调用形状长什么样(答案:[#16]–[#17])。\n\n下一步读者可以读 `experiments/02_read_local.py` 看读路径——那是 [#6] `read_lance_table` / [#7] `open_lance_dataset` 的使用现场。",
  "steps": [
    {
      "title": "动机: 为什么要给 Lance 开新路径",
      "description": "**核心结论:** 旧的 fat-client 路径对 Lance + 大规模 Ray 集群非常吃亏;`82cea51` 引入了一条 thin-client 路径,让用户在 worker 上自己写 Lance 目录,DeltaCat 只负责注册 manifest。\n\n**这一章要讲什么:**\n\n本章追踪两个 commit 的代码改动:\n\n- `82cea51` — Backport Lance integration(6 个文件,+1062 行)\n- `d6a4008` — 配套的 benchmark 脚本(experiments/ 目录)\n\n**三个痛点(旧 fat-client 路径在 Lance 场景下的问题):**\n\n1. **格式错位** — 用户的数据目标就是 Lance(O(1) 随机访问、向量检索用的列式格式);旧路径强迫它先变 Parquet。\n2. **集群利用率低** — 用户有 2800 CPU 的 Ray 集群,但 `dc.write_to_table(data=...)` 是单进程吃下整个 dataset,瓶颈在调用方那一个 Python 进程上。\n3. **Lance 是目录不是文件** — `ManifestEntry.from_path()` 假设 entry = 一个 file path;Lance dataset 是一整个目录(`_versions/`、`data/` 等),DeltaCat 自己去镜像 Lance 的 layout 是白干活。\n\n**Benchmark 佐证(来自 `experiment.md`):** thin-client 模式 12.5s / 572 MB/s,vs 单进程 `lance.write_dataset` 38.8s / 184 MB/s,加速 **3.1x**;scale 到 20 workers 达 18.9x。完整数字见 tour overview panel。\n\n**下面 [#2]–[#3] 先看最小的改动(两个 enum)——这是 Lance 在 DeltaCat 里获得身份的起点。**",
      "tags": ["动机", "role:entry", "priority:P1"],
      "color": "#f59e0b"
    },
    {
      "file": "experiment.md",
      "line": 27,
      "title": "动机: benchmark 数字在这里",
      "description": "**结论:** thin-client 模式把 344 个 episode 的写入从 38.8s 降到 12.5s(3.1x),worker 数从 1→20 几乎线性加速(18.9x)。这是整个 Lance 集成的价值证明。\n\n**关注的两张表:**\n\n- 2.1 节「写入 Benchmark」—— thin-client vs 单进程 `lance.write_dataset` 的直接对比。\n- 2.2 节「写入 Worker Scaling」—— 1 / 2 / 5 / 10 / 20 / 50 workers 的线性加速曲线。\n\n**为什么 20 workers 之后收敛到 22.4x?** 因为只有 344 个 episode,分到 50 个 worker 每人只有 6–7 个 episode,调度开销抵消了并行收益。对于大数据集(experiment.md 后面的 feb_08 / mar6 54M 行级别)scaling 会更持久。\n\n**本章用作 didactic 例子的这一次运行:**\n\n| 字段 | 值 |\n|---|---|\n| 数据集 | `feb2_small_batch`(344 episodes, 7.15 GB) |\n| 集群 | 33-node Ray, 2800 CPUs, Python 3.12 |\n| catalog root | `$OUTPUT/catalog/` |\n| data dir | `$OUTPUT/data/*.lance`(344 个目录) |\n| namespace | `mecka` |\n| table | `feb2_small_batch` |\n| content_type | `ContentType.LANCE` |\n\n所有后续 step 描述「本次取值」时,都指这次运行。",
      "tags": ["动机", "role:utility-dive", "priority:P3"],
      "color": "#9ca3af"
    },
    {
      "file": "deltacat/types/media.py",
      "line": 28,
      "location": "ContentType",
      "title": "类型枚举: ContentType.LANCE — 磁盘上的身份",
      "description": "**结论:** `ContentType.LANCE = 「application/vnd.lance」` 是 Lance 在 DeltaCat 体系里作为**磁盘格式**的身份证。只改了一行,但这行让 manifest entry 可以合法写 `content_type=ContentType.LANCE`。\n\n**这是什么:** `ContentType` enum 继承自 `str, Enum`,每个值是一个 media type 字符串,用于 ManifestEntry 的 `meta.content_type` 字段,告诉读端「这堆 bytes 该用什么格式来解」。\n\n**本次 diff 在这个文件里 touch 了 5 个位置(都是把 LANCE 加进各个 registry):**\n\n| 行 | 改动 | 作用 |\n|---|---|---|\n| 28 | `LANCE = 「application/vnd.lance」` | enum 本体 |\n| 66 | `「.lance」: ContentType.LANCE` | 扩展名 → enum 反查 |\n| 96 | `ContentType.LANCE.value` 加入 `SCHEMA_CONTENT_TYPES` | 标记「有 schema」 |\n| 115 | `ContentType.LANCE.value` 加入 `TABULAR_CONTENT_TYPES` | 标记「表格数据」 |\n| 137 | `DatasetType.LANCE` 见 [#3] | 另一个 enum |\n\n**和 [#3] 的区别:** 这里是**磁盘格式**(on-disk media type);[#3] 是**内存数据类型**(Python 对象)。一个 entry 可能 `content_type=LANCE`(磁盘存 Lance 目录)同时 `dataset_type=PYARROW`(读进内存是 PyArrow Table)——两个维度正交。",
      "tags": ["类型枚举", "role:main", "priority:P2"],
      "color": "#3b82f6"
    },
    {
      "file": "deltacat/types/media.py",
      "line": 137,
      "location": "DatasetType",
      "title": "类型枚举: DatasetType.LANCE — 内存里的身份",
      "description": "**结论:** `DatasetType.LANCE = 「lance」` 代表一个 `lance.LanceDataset` 句柄(lazy handle),它**不是**加载进内存的数据,而是一个支持 `take()`、`count_rows()`、`to_table(columns=...)` 的延迟对象。\n\n**这是什么:** `DatasetType` 枚举内存中的数据表示,每个值对应一种 Python 类型。\n\n| DatasetType | 对应 Python 类型 | 语义 |\n|---|---|---|\n| `LANCE` (新增) | `lance.LanceDataset` | **lazy** 句柄,O(1) 随机访问 |\n| `NUMPY` | `numpy.ndarray` | eager |\n| `PANDAS` | `pandas.DataFrame` | eager |\n| `POLARS` | `polars.DataFrame` | eager |\n| `PYARROW` | `pa.Table` | eager |\n| ...(还有远程的 RAY_DATASET 等) | | |\n\n**为什么 Lance 单独拿出来:** 其他 DatasetType 都是「数据已经在内存里」,Lance 是「元数据在内存、数据在磁盘,按需取」。代码里 `ds.take([12, 47, 203])` 会直接读那 3 行不用扫全表——这是视频训练随机采样的核心需求。\n\n**配合 [#7]`open_lance_dataset`:** 那是 Python 侧拿到 `lance.LanceDataset` 的入口。",
      "tags": ["类型枚举", "role:main", "priority:P2"],
      "color": "#3b82f6"
    },
    {
      "file": "deltacat/utils/lance_utils.py",
      "line": 1,
      "title": "lance_utils: 文件总览 — Lance 相关的 helper 都在这里",
      "description": "**结论:** 这个 723 行的新文件集中了 Lance 格式的所有 helper。外部调用最多的是两个:`build_manifest_entries`(thin-client 注册入口)和 `open_lance_dataset`(lazy 读)。其他都是围绕它们的支撑代码。\n\n**文件结构(按 section 划分):**\n\n| 行范围 | section | 主要内容 |\n|---|---|---|\n| 1–49 | 文件 docstring + 常量 | `DEFAULT_MAX_ROWS_PER_FILE = 100_000` |\n| 52–200 | **Internal helpers** | buffer alignment(见 [#8])、storage_options 构造 |\n| 203–376 | 继续 internal | `_storage_options_from_filesystem`, `_resolve_path`, `get_lance_dataset_stats`, `compute_sort_column_stats` |\n| **378–424** | **`build_manifest_entries`** ⭐ thin-client 桥梁 [#5] | |\n| **432–462** | **`read_lance_table`** [#6] | eager 读 |\n| **465–480** | **`open_lance_dataset`** [#7] | lazy 读 |\n| **488–605** | **`write_lance_table`** [#9 的变体] | table → Lance 目录 → manifest |\n| 608–663 | `write_lance_entries` | 批量写 |\n| 665–723 | `register_lance_dataset` | 高层注册入口 |\n\n**设计哲学:**\n\n- 公共 API 尽量**幂等**且**只做一件事**:`write_lance_table` 只负责写一个目录 + 返回 ManifestEntryList,注册是 catalog 层的事。\n- 跨进程安全:每个目录用 `uuid4()` 命名([#9]),Ray worker 并行写不会撞目录。\n- 失败可回滚:`write_lance_table` try/except 清理半写的 Lance 目录(见 [#9] 里的 cleanup 块)。\n\n下面 [#6]–[#9] 分别看这四个公共 helper。",
      "tags": ["lance_utils", "role:dispatch", "priority:P2"],
      "color": "#3b82f6"
    },
    {
      "file": "deltacat/utils/lance_utils.py",
      "line": 378,
      "location": "build_manifest_entries",
      "title": "lance_utils: build_manifest_entries — thin-client 的桥梁",
      "description": "**核心结论:** 接受一个**已经写好的** Lance 目录路径,返回一个 `ManifestEntryList`。用户把它交给 `dc.write_to_table(manifest=...)`,DeltaCat 就能在不读数据的前提下完成注册。这是 thin-client 模式成立的关键。\n\n**这个函数做什么:**\n\n签名:`build_manifest_entries(lance_path, filesystem=None, schema_id=None, ..., precomputed_stats=None) -> ManifestEntryList`\n\n读一个 Lance 目录的元数据(record count、content length、schema),构造一个 `ManifestEntry`,包进 `ManifestEntryList` 返回。**不读实际数据**,只读 Lance 自己的 manifest。\n\n**关注逻辑:**\n\n1. 第 402 行:`stats = precomputed_stats or get_lance_dataset_stats(lance_path, filesystem)` —— 如果调用方(比如 [#9]`write_lance_table`)已经拿过 stats 就复用,避免重开 dataset。\n2. 第 405–407 行:把 `lance_path` 改写成 **相对于 catalog_root** 的路径(多 catalog root 场景下的可移植性)。\n3. 第 409–419 行:构造 `ManifestMeta.of(...)`,填入各种元数据字段。\n4. 第 423 行:`ManifestEntry.of(url=url, meta=meta, mandatory=True)` —— 一个目录 = 一个 entry。\n\n**ManifestMeta.of(...) 本次填入的字段:**\n\n| 字段 | 来源 | 本次例子的值 |\n|---|---|---|\n| `record_count` | Lance dataset `count_rows()` | 每个 episode 1 行 |\n| `content_length` | 遍历 fragment 算出来的总字节 | 每个 episode 几十 MB(视频) |\n| `content_type` | 硬编码 `ContentType.LANCE.value` | `「application/vnd.lance」` |\n| `content_encoding` | 硬编码 `IDENTITY` | 不压缩 |\n| `source_content_length` | 调用方传(可选) | 内存中 PyArrow Table 的大小 |\n| `schema_id` | 显式传 or Lance Arrow metadata 里抠出来(见 [#9]) | 这里 `None`(简单场景) |\n| `sort_scheme_id` | 调用方传 | `None` |\n| `entry_type` | 默认 `EntryType.DATA` | `DATA` |\n\n**和 `ManifestEntry.from_path()` 的对比(见 docstring 第 396 行):**\n\n| | `ManifestEntry.from_path(file_path)` | `build_manifest_entries(lance_path)` |\n|---|---|---|\n| 输入 | 单个文件 | 一个目录 |\n| content_length | 文件大小 | 目录下所有 fragment 的字节总和(S3-consistent) |\n| 适用于 | Parquet / Feather / ... | **Lance**(directory-based format) |\n\n**本次 01_write_local.py 怎么用(见 [#16]):** 循环 344 次,每次传一个 `$OUTPUT/data/EPISODE_XYZ.lance` 目录路径,收集 344 个 ManifestEntry,合并成一个 manifest 交给 `write_to_table`。",
      "tags": ["lance_utils", "role:main", "priority:P0"],
      "color": "#ef4444"
    },
    {
      "file": "deltacat/utils/lance_utils.py",
      "line": 432,
      "location": "read_lance_table",
      "title": "lance_utils: read_lance_table — eager 读一整个 Lance 目录",
      "description": "**结论:** 一次性把 Lance dataset 读成 `pa.Table`,支持列投影和 filter 下推。用于「我要所有数据」的场景。\n\n**这个函数做什么:**\n\n签名:`read_lance_table(path, filesystem=None, catalog_root=None, include_columns=None, row_filter=None, data_root=None) -> pa.Table`\n\n内部两步:\n1. 第 446–447 行:解析路径 + 构造 Lance 能识别的 storage_options(S3 credential 等)。\n2. 第 448 行:`ds = lance.dataset(path, ...)` 开 dataset 句柄。\n3. 第 462 行:`ds.to_table(**kwargs)` —— **materialize 全部数据**。\n\n**关注逻辑:**\n\n- `include_columns` 映射到 Lance 的 `columns=[...]` 参数 —— 列投影在 Lance 内部发生,避免读 disk 上的其他列。\n- `row_filter` 映射到 Lance 的 `filter=\"...\"` 参数 —— predicate pushdown。\n\n**和 [#7] `open_lance_dataset` 的区别:**\n\n| | `read_lance_table` | `open_lance_dataset` |\n|---|---|---|\n| 返回类型 | `pa.Table`(在内存里) | `lance.LanceDataset`(lazy 句柄) |\n| 内存占用 | O(全部数据) | O(1) |\n| 用途 | 「我要全读」 | 随机采样、训练 dataloader |\n\n**本次 experiment.md 2.3 节的对照数字:**\n\n| 场景 | 时间 |\n|---|---|\n| Eager full(7.15 GB 全读) | 本地 24.1s / S3 33.3s |\n| Lazy `take(10 random)`(见 [#7]) | 本地 0.34s / S3 7.9s |",
      "tags": ["lance_utils", "role:main", "priority:P2"],
      "color": "#3b82f6"
    },
    {
      "file": "deltacat/utils/lance_utils.py",
      "line": 465,
      "location": "open_lance_dataset",
      "title": "lance_utils: open_lance_dataset — lazy 句柄, O(1) 随机访问",
      "description": "**结论:** 不加载数据,只拿到 `lance.LanceDataset` 句柄。用于训练 dataloader 需要随机采样的场景。\n\n**这个函数做什么:**\n\n签名:`open_lance_dataset(path, filesystem=None, catalog_root=None, data_root=None) -> lance.LanceDataset`\n\n只有 3 行:解析路径、构造 storage_options、`lance.dataset(path, storage_options=...)`。关键是**不调用** `.to_table()`——返回的是一个句柄,实际读在后面每次 `take()` 时发生。\n\n**句柄上的三个核心方法(见 docstring 第 473–476 行):**\n\n| 方法 | 语义 | 用途 |\n|---|---|---|\n| `ds.take(indices)` | O(1) 随机访问指定行索引 | 训练 dataloader,每个 batch 随机采 N 行 |\n| `ds.count_rows()` | 从 manifest 读行数,**不读数据** | dataset 大小 |\n| `ds.to_table(columns=[...])` | 选择性 materialize 指定列 | 只要 metadata 列(跳过 video) |\n\n**为什么这个函数值得单独存在:** 它是 `DatasetType.LANCE`([#4])在运行时的对应物。整个 DeltaCat 的读路径,当 entry `content_type == LANCE` 时,走这条 lazy 路径。\n\n**experiment.md 2.3 节佐证(S3):** `Lazy column projection(2 cols) 1.6s` vs `Eager full 33.3s` —— 20 倍差距,这就是 lazy 的价值。",
      "tags": ["lance_utils", "role:main", "priority:P2"],
      "color": "#3b82f6"
    },
    {
      "file": "deltacat/utils/lance_utils.py",
      "line": 149,
      "location": "_ensure_buffer_alignment",
      "title": "lance_utils: buffer alignment — 一个容易踩的 Rust FFI 坑",
      "description": "**结论:** Lance 用 Rust 写的,通过 arrow-rs 的 FFI 读 PyArrow buffer;arrow-rs 的 `ScalarBuffer<T>` 要求 buffer 指针对齐到 `std::mem::align_of::<T>()`,而 PyArrow 经过 schema 转换后的 buffer 可能不满足这个对齐——不处理会在写入时 panic。\n\n**这一段做什么:**\n\n`_ensure_buffer_alignment(table)` 检查每列是否对齐,只对需要的列做 realignment,保留对齐的列原样(fast path)。\n\n**关注逻辑(两阶段):**\n\n1. **检测阶段**(第 175–179 行):对每列调用 `_column_needs_realignment`,收集 `misaligned` 列表。\n2. **修复阶段**(第 194–200 行):只对 `misaligned` 里的列调 `_realign_column`,其余保持引用。\n\n**关注变量:**\n\n| 变量 | 含义 | 作用 |\n|---|---|---|\n| `_TYPE_ALIGNMENT`(第 67 行) | `{pa type id: alignment bytes}` 映射 | int16→2, int64→8, decimal128→16, ... |\n| `misaligned`(第 175 行) | 需要 realign 的列名列表 | 大多数情况是空 |\n\n**性能表(见第 161–169 行 docstring 引用):**\n\n| Rows | 旧 `from_pydict` | 新靶向修复 | 加速 |\n|---|---|---|---|\n| 1 K | 2.5 ms | 0.05 ms | 47x |\n| 100 K | 324 ms | 0.05 ms | 6,500x |\n| 1 M | 3,414 ms | 0.05 ms | 65,000x |\n\n**背景(第 60–66 行):**\n\n- 根因 upstream issue: https://github.com/apache/arrow-rs/issues/7136\n- Lance 3.0.0 用 arrow-rs v57.x,不自动对齐 FFI buffer。\n- 维护者关了那个 PR,理由是「opt-in 更好」——于是 DeltaCat 只能在 Python 侧自己处理。\n\n这是 subtle gotcha,平时没人想得到,但凡经过 schema 强转(比如 `table.cast(new_schema)`)就可能触发。",
      "tags": ["lance_utils", "role:utility-dive", "priority:P3"],
      "color": "#9ca3af"
    },
    {
      "file": "deltacat/utils/lance_utils.py",
      "line": 488,
      "location": "write_lance_table",
      "title": "lance_utils: write_lance_table — 一把梭: 写目录 + 建 entry",
      "description": "**结论:** 上层便捷入口 —— 传一个 `pa.Table` 和 `base_path` 进去,得到一个 `ManifestEntryList`。它本质上是 `lance.write_dataset(...)` + `build_manifest_entries(...)` 的组合,再加上 schema_id 嵌入、buffer 对齐、失败清理。\n\n**这个函数做什么:**\n\n签名:\n```\nwrite_lance_table(\n    table: pa.Table,\n    base_path: str,\n    filesystem: Optional[pafs.FileSystem] = None,\n    catalog_root: Optional[str] = None,\n    schema_id: Optional[int] = None,\n    sort_scheme_id: Optional[str] = None,\n    sort_column: Optional[str] = None,\n    max_rows_per_fragment: int = 100_000,  # DEFAULT_MAX_ROWS_PER_FILE\n    entry_type: Optional[EntryType] = EntryType.DATA,\n    entry_params: Optional[Any] = None,\n    data_root=None,\n) -> ManifestEntryList\n```\n\n**关注逻辑(5 个阶段):**\n\n| 阶段 | 行 | 做什么 |\n|---|---|---|\n| 1. 路径命名 | 517–518 | `dataset_name = f「{uuid4()}.lance」` — 每次 UUID 避免跨 worker 撞目录 |\n| 2. schema_id 嵌入 | 521–529 | 把 DeltaCat 的 schema_id 塞进 Arrow schema metadata(`DELTACAT:schema_id` key),让它跨 Lance manifest 存活 |\n| 3. buffer 对齐 | 537 | 调 [#8]`_ensure_buffer_alignment` |\n| 4. 真正写 | 544–570 | `lance.write_dataset(table, lance_path, mode=「create」, max_rows_per_file=...)`,失败时 cleanup 半写的目录 |\n| 5. 建 entry | 594–605 | 调 [#5]`build_manifest_entries`,复用阶段 3–4 期间算出的 stats |\n\n**关注变量:**\n\n| 变量 | 来源 | 本次取值(假想一个 100K 行的 batch) |\n|---|---|---|\n| `dataset_name` | 517 | `「a3f1...b9c.lance」` |\n| `lance_path` | 518 | `/output/data/a3f1...b9c.lance` |\n| `max_rows_per_fragment` | 参数 | **100_000**(DEFAULT_MAX_ROWS_PER_FILE) |\n| `source_content_length` | 542 | 内存中 table 大小,在写入**之前** capture 住 |\n\n**为什么 `max_rows_per_fragment = 100_000` 而不是 Lance 默认的 1M(第 42–48 行 docstring 讲理由):**\n\n- 多模态数据(PackDS)每行 5 KB–500 KB;100K 行 = 500 MB–50 GB。\n- Lance 默认 1M 行 × 500 KB = 500 GB 单文件,超过 compactor 的 memory budget。\n- 小于这个值也不好:1K 行单文件会造成碎片。\n\n**失败清理(第 552–569 行)的关键理由:**\n\n> 「A partial `.lance` directory left on disk could appear valid to subsequent reads, unlike a truncated Parquet file which is obviously invalid.」\n\n半写的 Parquet 文件下次读会报 magic byte 错,半写的 Lance 目录可能还能「读出半个 fragment」——更危险,必须清掉。\n\n**本次 01_write_local.py 没有用这个函数**,而是直接调 `lance.write_dataset` + [#5] `build_manifest_entries`。这个函数是给 DeltaCat 自己的 writer layer 用的。",
      "tags": ["lance_utils", "role:main", "priority:P2"],
      "color": "#3b82f6"
    },
    {
      "file": "deltacat/storage/main/impl.py",
      "line": 2661,
      "location": "stage_delta",
      "title": "Storage: 旧的 stage_delta — 和新的对照着看",
      "description": "**结论:** 旧 `stage_delta(data, partition, ...)` 是 fat-client 模式的心脏:输入是 raw data,内部调 `_write_table` 序列化 + 写文件,再把写文件过程中生成的 manifest 包进 `Delta` 返回。一共 76 行。[#12] 的新函数只留了其中 12 行。\n\n**这个函数做什么:**\n\n签名:`stage_delta(data, partition, delta_type, max_records_per_entry, ..., content_type=PARQUET, ...) -> Delta`\n\n它返回一个**未注册**的 Delta —— 注册是由后续的 `commit_delta` 做的。\n\n**关注逻辑(5 步):**\n\n| 步骤 | 行 | 做什么 |\n|---|---|---|\n| 1. content_type 校验 | 2685–2689 | `partition.is_supported_content_type(content_type)` |\n| 2. partition 状态检查 | 2690–2693 | 不能写到 DEPRECATED partition |\n| 3. schema 处理 | 2702–2711 | 把 DeltaCat Schema 的 arrow 版本和 schema_id 塞到 `table_writer_kwargs` |\n| 4. **实际写数据** | 2717–2728 | `_write_table(partition.partition_id, data, max_records_per_entry, ..., **kwargs)` —— **这一步序列化 + 写文件 + 建 manifest** |\n| 5. 包成 Delta | 2729–2737 | `Delta.of(locator=..., manifest=manifest, ...)` |\n\n**关键:第 2717–2728 行的 `_write_table` 是 fat-client 模式最吃 CPU 的地方**—— 对 344 个 episode 的 7.15 GB 视频数据来说,这里会是 38.8 秒的主要来源。\n\n**下一步 [#12] 是 `stage_delta_from_manifest`,把这 76 行砍到 12 行——砍掉的正好是上面步骤 3 和 4(schema 处理和 `_write_table`),因为 manifest 路径下这些事调用方已经做完了。**",
      "tags": ["Storage", "role:branch-skipped", "priority:P2"],
      "color": "#3b82f6"
    },
    {
      "file": "deltacat/storage/main/impl.py",
      "line": 2740,
      "location": "stage_delta_from_manifest",
      "title": "Storage: stage_delta_from_manifest — thin-client 的核心 (新增)",
      "description": "**核心结论:** 新函数,**接受一个已经构造好的 `Manifest`,直接包成 `Delta` 返回**。相比 [#11]`stage_delta` 砍掉了所有「把 data 变成文件」的步骤——因为在 thin-client 模式下,文件早就被调用方(Ray worker)写好了。\n\n**这个函数做什么:**\n\n签名:\n```\nstage_delta_from_manifest(\n    manifest: Manifest,                        # 调用方已经构造好\n    partition: Partition,\n    delta_type: DeltaType = DeltaType.UPSERT,\n    properties: Optional[DeltaProperties] = None,\n    content_type: ContentType = ContentType.PARQUET,\n    schema: Optional[Schema] = None,           # 接收但不使用\n    *args, **kwargs,\n) -> Delta\n```\n\n**关注逻辑:**\n\n对照 [#11] 的 5 步:\n\n| 步骤 | 旧 `stage_delta` | 新 `stage_delta_from_manifest` | 差异 |\n|---|---|---|---|\n| 1. content_type 校验 | ✅ 2685 | ✅ 2751 | 一样 |\n| 2. partition 状态检查 | ✅ 2690 | ✅ 2756 | 一样 |\n| 3. schema 处理 | ✅ 2702–2711 | ❌ **skip** | manifest 里已经包含 schema_id |\n| 4. 写数据(`_write_table`) | ✅ 2717–2728 | ❌ **skip** | 核心差异 |\n| 5. 包成 Delta | ✅ 2729–2737 | ✅ 2763–2770 | 一样,但用**传进来的 manifest** 而不是写出来的 manifest |\n\n**关注变量:**\n\n| 变量 | 来源 | 在本次 [#16] `01_write_local.py` 的取值 |\n|---|---|---|\n| `manifest` | 调用方传入 | 包含 344 个 ManifestEntry(每个 episode 一个) |\n| `partition` | catalog 层传入 | 新建的 default partition |\n| `delta_type` | 默认 `UPSERT` | `UPSERT` |\n| `content_type` | 调用方传入 | `ContentType.LANCE` |\n| `manifest.meta` | 来自 [#5] | 合并了 344 个 entry 的 meta(total record_count / content_length) |\n\n**返回值:** `Delta.of(locator=DeltaLocator.of(partition.locator, None), delta_type=UPSERT, meta=manifest.meta, properties=None, manifest=manifest, previous_stream_position=<prev>)` —— 一个未注册的 staged Delta,等 [#15] `commit_delta` 把它持久化。\n\n**为什么这是 P0:** 这是 thin-client 路径在 storage 层的**唯一新增入口**。所有「绕过数据处理」的好处,最后都落在「调的是这个函数而不是 [#11]」上。",
      "tags": ["Storage", "role:main", "priority:P0"],
      "color": "#ef4444"
    },
    {
      "file": "deltacat/catalog/main/impl.py",
      "line": 231,
      "location": "write_to_table",
      "title": "Catalog: write_to_table 签名改动 — data 不再是必填",
      "description": "**结论:** `write_to_table` 的签名改了两处关键:`data: Dataset = None`(之前必填)、新增 `manifest: Optional[Any] = None`。这两个改动让「只传 manifest 不传 data」成为合法调用。\n\n**本次 diff 的签名变化(行 232–240):**\n\n```python\ndef write_to_table(\n    data: Dataset = None,          # ← 变了: 原本 `data: Dataset` (必填,位置参数)\n    table: str = \"\",               # ← 也加了默认值\n    *args,\n    namespace: Optional[str] = None,\n    table_version: Optional[str] = None,\n    mode: TableWriteMode = TableWriteMode.AUTO,\n    content_type: ContentType = ContentType.PARQUET,\n    manifest: Optional[Any] = None,  # ← 新增参数\n    transaction: Optional[Transaction] = None,\n    **kwargs,\n) -> List[Delta]:\n```\n\n**为什么这个改动重要:**\n\n- 旧调用:`dc.write_to_table(my_df, \"my_table\")` —— `data` 和 `table` 是位置参数,`data` 不能 `None`。\n- 新调用([#16]):`dc.write_to_table(data=None, table=TABLE_NAME, manifest=Manifest.of(entries=...), content_type=ContentType.LANCE, ...)` —— 显式传 `data=None`,内容通过 `manifest` 传递。\n\n**关注的 3 个关键节点(下面依次看):**\n\n| 节点 | 行 | 作用 | Step |\n|---|---|---|---|\n| 签名改动 | 232–240 | 让 `data=None, manifest=...` 合法 | 本 step |\n| `_create_table_for_write` 里 schema 分支 | 207–213 | 没 data 时不去 infer schema | [#14] |\n| `if manifest is not None` 短路分支 | 382–400 | 直接 stage_delta_from_manifest + commit + return | [#15] |\n\n**P0 严重性判据:** 这是 public API 的向后不兼容改动(从 positional 变成 keyword-optional)。下游任何依赖 `dc.write_to_table(data, table, ...)` 位置参数的代码都需要检查——但对 Python keyword-only 调用是安全的。",
      "tags": ["Catalog", "role:main", "priority:P0"],
      "color": "#ef4444"
    },
    {
      "file": "deltacat/catalog/main/impl.py",
      "line": 207,
      "location": "_create_table_for_write",
      "title": "Catalog: _create_table_for_write — schema 推断的三叉分支",
      "description": "**结论:** 新建 table 时,schema 从哪来?以前只有一条路(`infer_table_schema(data)`)。现在变成三叉:有 data 走推断,有 manifest 走 pass(schema_id 在 manifest 里),两个都没有再走旧路径。\n\n**这一段做什么:**\n\n当用户 `dc.write_to_table(...)` 的目标 table **不存在**时,DeltaCat 要先建表,建表需要一个 schema。`_create_table_for_write` 就是这个入口。\n\n**关注逻辑(第 207–213 行的三叉 if):**\n\n```python\nif \"schema\" not in kwargs:\n    if data is not None:\n        kwargs[\"schema\"] = infer_table_schema(data)    # 旧路径\n    elif kwargs.get(\"manifest\") is not None:\n        pass                                            # 新路径: manifest 模式跳过\n    else:\n        kwargs[\"schema\"] = infer_table_schema(data)    # fallback(会 infer None,后续报错)\n```\n\n**三条分支的实际含义:**\n\n| 分支 | 触发条件 | schema 从哪来 |\n|---|---|---|\n| `data is not None` | 传了 DataFrame/Table | `infer_table_schema(data)` 扫描 data |\n| `manifest is not None` | thin-client 模式 | **跳过**,从 manifest 的 entry meta 里读 `schema_id` |\n| 都没有 | 异常路径 | 走 `infer_table_schema(None)`,下面会抛错 |\n\n**本次 [#16] `01_write_local.py` 走哪条:** `manifest is not None` 分支。因为 344 个 Lance 目录里,每个 entry 的 `meta.schema_id` 在 [#5] `build_manifest_entries` 里已经被嵌进去了,DeltaCat 不需要再从 data 推。\n\n**注意:** 这里的 `kwargs.get(\"manifest\")` 是在 [#13] 调用处的第 287 行 `kwargs[\"manifest\"] = manifest` 显式塞进 kwargs 的——不是 `write_to_table` 签名里的 `manifest` 参数自动透传。",
      "tags": ["Catalog", "role:branch-taken", "priority:P1"],
      "color": "#f59e0b"
    },
    {
      "file": "deltacat/catalog/main/impl.py",
      "line": 382,
      "location": "write_to_table",
      "title": "Catalog: if manifest is not None — 短路到 commit (新增)",
      "description": "**核心结论:** 这是 thin-client 模式在 catalog 层的**唯一新增入口**。一旦命中 `manifest is not None`,后续所有「fat-client 要做的事」——schema evolution、data type conversion、compaction 触发——全部跳过,直接 stage_delta_from_manifest → commit → return。\n\n**这一段做什么:**\n\n```python\n# --- Manifest-only (thin-client) write path ---\n# When a pre-built manifest is provided, the client has already\n# written data files. Skip data processing and go to commit.\nif manifest is not None:\n    from deltacat.storage.main.impl import (\n        stage_delta_from_manifest,\n        commit_delta as storage_commit_delta,\n        commit_partition as storage_commit_partition,\n    )\n\n    delta = stage_delta_from_manifest(\n        manifest=manifest,\n        partition=partition,\n        delta_type=delta_type,\n        content_type=content_type,\n    )\n    delta = storage_commit_delta(delta=delta, **kwargs)\n\n    if commit_staged_partition:\n        storage_commit_partition(partition=partition, **kwargs)\n\n    return [delta]\n```\n\n**关注逻辑(3 步):**\n\n1. **stage**(第 389–394 行)—— 调 [#12]`stage_delta_from_manifest`,把 manifest 包成 staged Delta。\n2. **commit**(第 395 行)—— `commit_delta` 把 Delta 持久化到 catalog(metafile 写盘 + 事务提交)。\n3. **提交 partition**(第 397–398 行)—— 如果这次调用还 staged 了一个新 partition,一并 commit。\n4. **提前返回**(第 400 行)—— 直接 `return [delta]`,**下面所有 fat-client 逻辑都不执行**。\n\n**被跳过的下面那些步骤(第 402 行之后,见 catalog/main/impl.py):**\n\n| 跳过了什么 | 作用(在 fat-client 模式下) | 为什么 manifest 模式不需要 |\n|---|---|---|\n| `schema_evolution_mode` 读取 + 校验 | 防止 schema drift | 调用方已保证 schema 一致(schema_id 在 manifest 里) |\n| data→table 的类型转换 | 把 DataFrame 转成 PyArrow | 没 data 要转 |\n| `stage_delta(data, ...)` | 序列化 + 写文件 | 文件已经由 Ray worker 写完 |\n| compaction 触发逻辑 | 碎片整理 | 调用方自己决定,不强制 |\n\n**关注变量:**\n\n| 变量 | 在本次 [#16] 的取值 |\n|---|---|\n| `manifest` | `Manifest.of(entries=<344 个 ManifestEntry>)` |\n| `partition` | 新 staged 的 default partition(`commit_staged_partition=True`) |\n| `delta_type` | `DeltaType.APPEND`(因为 mode=CREATE 是新表) |\n| `content_type` | `ContentType.LANCE` |\n| `kwargs[\"transaction\"]` | write_to_table 开的事务 |\n| 返回值 | `[delta]` —— 单元素 list,一个 commit 过的 Delta |\n\n**P0 严重性判据:** 这是整个 Lance 集成的**最重要一行**——if 的条件决定了走哪条路。任何 thin-client 调用都会在这一行分叉。",
      "tags": ["Catalog", "role:main", "priority:P0"],
      "color": "#ef4444"
    },
    {
      "file": "experiments/01_write_local.py",
      "line": 78,
      "location": "main",
      "title": "用户侧: 01_write_local.py main — thin-client 典型调用",
      "description": "**结论:** 这是 thin-client 模式最干净的调用样板 —— 两阶段(Ray 并行写 Lance + DeltaCat 注册 manifest),一共 60 行,把上面整个 commit 的 API 都用上了。\n\n**这个脚本做什么:**\n\n读 `/mnt/amlfs-07/shared/datasets/.../mecka/feb2_small_batch` 下的 344 个 episode 目录(每个含 `metadata.json` + `video.mp4`),把每个 episode 写成一个 Lance dataset,然后注册到 DeltaCat catalog。\n\n**关注逻辑(2 phase):**\n\n| Phase | 行 | 做什么 |\n|---|---|---|\n| 1 | 94–101 | 用 Ray 并行写 Lance 目录(344 个 task)|\n| 2 | 104–127 | 建 catalog + namespace,收集 manifest entries,调 `write_to_table` |\n\n**关注变量(写路径相关):**\n\n| 变量 | 定义 | 本次取值 |\n|---|---|---|\n| `SOURCE_DIR` | 行 25 | `.../mecka/feb2_small_batch` |\n| `DATA_DIR` | 行 27 | `$OUTPUT/data/`(Lance 目录落在这里) |\n| `CATALOG_ROOT` | 行 28 | `$OUTPUT/catalog/` |\n| `NAMESPACE` | 行 30 | `「mecka」` |\n| `TABLE_NAME` | 行 31 | `「feb2_small_batch」` |\n| `SCHEMA` | 行 33–42 | 8 列:episode_id / user_id / environment_id / scene_id / task_id / duration / video_bytes(large_binary) / preannotations_json |\n| `futures` | 行 95 | 344 个 Ray task future |\n| `results` | 行 96 | 344 条 `{lance_path, episode_id, video_size}` dict |\n| `all_entries` | 行 111–114 | 循环调 [#5]`build_manifest_entries` 收集出 344 个 ManifestEntry |\n\n**下一步 [#17] 看每个 Ray worker 里发生了什么。**",
      "tags": ["用户侧", "role:main", "priority:P1"],
      "color": "#f59e0b"
    },
    {
      "file": "experiments/01_write_local.py",
      "line": 119,
      "location": "main",
      "title": "用户侧: dc.write_to_table 的 manifest 调用形状",
      "description": "**结论:** `dc.write_to_table(data=None, manifest=Manifest.of(entries=...), content_type=ContentType.LANCE, ...)` —— 这一行命中 [#15] 的 `if manifest is not None` 分支,整个 commit 是为了让这一行能工作。\n\n**这一段做什么:**\n\n```python\ndc.write_to_table(\n    data=None,                                       # ← 关键: 显式传 None\n    table=TABLE_NAME,                                # 「feb2_small_batch」\n    namespace=NAMESPACE,                             # 「mecka」\n    manifest=Manifest.of(entries=merged),            # ← 344 个 ManifestEntry\n    content_type=ContentType.LANCE,                  # ← 触发 LANCE 路径\n    mode=TableWriteMode.CREATE,                      # 新建 table\n    catalog=CATALOG_NAME,                            # 「tutorial」\n)\n```\n\n**关注变量(每个参数的来源):**\n\n| 参数 | 来源 | 在这次运行的实际值 |\n|---|---|---|\n| `data` | 硬编码 `None` | `None`(显式) |\n| `table` | 行 31 的常量 | `「feb2_small_batch」` |\n| `namespace` | 行 30 的常量 | `「mecka」` |\n| `manifest` | `Manifest.of(entries=merged)`,`merged` 由行 117 合并 344 个 entries | `Manifest` 包含 344 个 ManifestEntry,合计 7.15 GB |\n| `content_type` | 硬编码 | `ContentType.LANCE`(磁盘格式,见 [#2]) |\n| `mode` | 硬编码 | `TableWriteMode.CREATE`(因为脚本假设 table 不存在) |\n| `catalog` | 行 29 | `「tutorial」`(之前 `dc.put_catalog(CATALOG_NAME, ...)` 注册的) |\n\n**调用链路:**\n\n```\nmain (本行)\n  └─ dc.write_to_table [#13]\n       ├─ _create_table_for_write [#14]    ← schema 分支走 manifest 那条\n       ├─ _handle_write_mode                ← 创建 partition\n       └─ if manifest is not None: [#15]    ← 短路到这里\n            ├─ stage_delta_from_manifest [#12]\n            ├─ commit_delta\n            └─ commit_partition (if staged)\n```\n\n**如果这行写成 fat-client 会怎样?**\n\n```python\n# 假想的旧写法(不再可行,因为 344 个 Lance dataset 不是一个 in-memory DataFrame)\n# data = <某种合并后的 PyArrow Table,7.15 GB 在内存里>\n# dc.write_to_table(data, \"feb2_small_batch\", ...)\n```\n\n不仅占 7.15 GB 内存,而且单进程 serialize 成 Parquet 要 38.8s(experiment.md 2.1)。thin-client 模式把这一段从 38.8s 降到 12.5s + 注册阶段的几百毫秒。",
      "tags": ["用户侧", "role:call-site", "priority:P0"],
      "color": "#ef4444"
    },
    {
      "file": "deltacat/io/datasource/deltacat_datasource.py",
      "line": 20,
      "title": "兼容: Ray 2.54 FastFileMetadataProvider try/except fallback",
      "description": "**结论:** 顺手打的补丁 —— Ray 2.54 把 `FastFileMetadataProvider` 和 `ParquetMetadataProvider` 从 `ray.data.datasource` 移除了,DeltaCat 加一个 try/except 回落到 `BaseFileMetadataProvider`,让 import 不再挂掉。和 Lance 本身没关系,只是同一个升级周期里遇到的。\n\n**这一段做什么:**\n\n```python\ntry:\n    from ray.data.datasource import (\n        FastFileMetadataProvider,\n        ParquetMetadataProvider,\n    )\nexcept ImportError:\n    from ray.data.datasource import (\n        BaseFileMetadataProvider as FastFileMetadataProvider,\n    )\n\n    class ParquetMetadataProvider(FastFileMetadataProvider):  # type: ignore[no-redef]\n        pass\n```\n\n**关注逻辑:**\n\n- **正常路径(Ray ≤ 2.53):** 直接从 `ray.data.datasource` 导入两个 class。\n- **fallback 路径(Ray 2.54+):** `FastFileMetadataProvider` 别名到 `BaseFileMetadataProvider`(功能上是父类),`ParquetMetadataProvider` 做成一个空 subclass(dummy,保持 API 形状兼容)。\n\n**副作用(下面第 66 行):**\n\n原本 `CachedFileMetadataProvider` 多继承两个 provider,diff 里也把 `ParquetMetadataProvider` 从继承链去掉了(因为它现在可能只是 dummy),保留单继承 `FastFileMetadataProvider`。\n\n**为什么 P3:** 这是环境兼容补丁,跟 Lance 语义无关。但 experiment.md 头部写的是 `ray 2.54`,所以不打这个补丁连 `import deltacat` 都跑不起来——于是跟 Lance 集成一起 backport 了。",
      "tags": ["兼容", "role:utility-dive", "priority:P3"],
      "color": "#9ca3af"
    },
    {
      "title": "收束: 一句话总结 + 下一步",
      "description": "**一句话总结:** `82cea51` 给 DeltaCat 开了一条**thin-client 写入路径**—— 用户(或 Ray worker)在外部写好 Lance 目录,调用 [#5]`build_manifest_entries` 建 ManifestEntry,把合并后的 Manifest 交给 `dc.write_to_table(data=None, manifest=..., content_type=ContentType.LANCE)`,DeltaCat 在 [#15]`if manifest is not None` 分支短路到 [#12]`stage_delta_from_manifest`,绕过所有数据处理直接 commit。benchmark 给到 3.1x 加速(344 task)和 18.9x worker scaling(20 workers)。\n\n**关键交叉引用速查:**\n\n| 要读什么 | 跳到 |\n|---|---|\n| 为什么要这样做 | [#1] + tour overview 的第 1–3 节 |\n| Lance 的两个身份 | [#3](磁盘)+ [#4](内存) |\n| thin-client 的桥梁函数 | [#5] `build_manifest_entries` |\n| commit 的核心 | [#12] `stage_delta_from_manifest` |\n| 路径分叉点 | [#15] `if manifest is not None` |\n| 用户怎么调 | [#16] + [#17] |\n\n**下一步推荐阅读:**\n\n- `experiments/02_read_local.py` —— 读路径的 thin-client 例子,用到 [#7] `open_lance_dataset` + `ds.take([...])` 做随机采样。\n- `experiments/08_lancedb_vs_deltacat_write.py` —— 和 LanceDB 直写的对照,验证 DeltaCat 版本管理那一层的开销。\n- `experiment.md` 第 3、4 节 —— 大数据集(35 GB / 18.5 GB)的 scaling 曲线,以及关于「shard 数量不要用 ceil(bytes/512MB) 公式」的讨论。\n\n**还没讲到但值得知道的细节:**\n\n- `register_lance_dataset`(lance_utils.py 第 665 行)—— 更高阶封装,把 `build_manifest_entries` + `dc.write_to_table` 打包成一个调用,内部也走 [#15] 那条分支。\n- `write_lance_entries`(lance_utils.py 第 608 行)—— 批量版的 `write_lance_table`,用于一次写多个 Lance dataset。\n- `compute_sort_column_stats`(第 333 行)—— 为 copy-by-reference compaction 预计算 sort 列的 min/max。\n\n这些都沿着 thin-client 同一条哲学线展开,读者感兴趣可以继续深入。",
      "tags": ["收束", "role:return", "priority:P1"],
      "color": "#f59e0b"
    }
  ]
}
