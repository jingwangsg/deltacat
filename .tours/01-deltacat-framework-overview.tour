{
  "$schema": "https://aka.ms/codetour-schema",
  "title": "1 - DeltaCat 框架分层总览",
  "description": "给大一同学看的 DeltaCat 架构导览 —— 从 import 语句到磁盘上的文件",
  "overview": "# Tour · DeltaCat 框架分层总览\n\n这是一份给**本科一年级同学**的框架导览。目标:读完以后你能画出 DeltaCat 的分层架构图,并知道每一层干什么。\n\n> 不讲 Lance 集成(另外一章讲),也不讲 compaction 的具体算法。这一章只讲「一个写入请求在 DeltaCat 里是怎么分层走的」。\n\n---\n\n## DeltaCat 在解决什么问题\n\n想象你在管理一个巨大的数据湖(data lake)—— 几百 TB 的视频、图像、表格数据堆在 S3 或本地磁盘上。你希望:\n\n1. 给这些数据**起人类可读的名字**(而不是一堆 UUID 路径)。\n2. 能**追溯历史**(今天写的是哪个版本?上周写的还在吗?像 git 一样)。\n3. 支持**并发写入**(多个 Ray worker 同时写,不会互相踩踏)。\n4. 背后文件格式可以是 Parquet / Lance / Feather / CSV ……调用方不需要关心。\n\nDeltaCat 就是干这件事的**数据湖引擎**(data lake engine):给 raw 文件套上一层「目录 + 事务 + 版本」。\n\n---\n\n## 大局观 —— 五层架构\n\n```\n┌─────────────────────────────────────────────────────┐\n│  用户代码:  import deltacat as dc                  │\n│              dc.write_to_table(data, 「my_table」)  │\n└───────────────────┬─────────────────────────────────┘\n                    │ 看到 [#2][#3]\n                    ▼\n┌─────────────────────────────────────────────────────┐\n│  第 1 层  Catalog 层  (目录 / 入口)                 │\n│  deltacat/catalog/                                  │\n│    __init__.py   —— 用户的 API 入口 [#4]           │\n│    delegate.py   —— 路由到当前 catalog 实现 [#5]   │\n│    main/impl.py  —— 默认实现:写流程协调 [#6]      │\n└───────────────────┬─────────────────────────────────┘\n                    │\n                    ▼\n┌─────────────────────────────────────────────────────┐\n│  第 2 层  Storage 层  (五级元数据模型)              │\n│  deltacat/storage/model/                            │\n│    Namespace  → Table → TableVersion → Stream       │\n│              → Partition → Delta → Manifest         │\n│                                             [#7]–[#15] │\n└───────────────────┬─────────────────────────────────┘\n                    │\n                    ▼\n┌─────────────────────────────────────────────────────┐\n│  第 3 层  Types 层  (类型枚举)                      │\n│  deltacat/types/media.py                            │\n│    ContentType   —— 磁盘上的文件格式 [#16]         │\n│    DatasetType   —— 内存里的数据类型 [#17]         │\n└───────────────────┬─────────────────────────────────┘\n                    │\n                    ▼\n┌─────────────────────────────────────────────────────┐\n│  第 4 层  IO / Utils 层  (真正读写文件的地方)        │\n│  deltacat/io/   —— 读写适配器 [#18]                │\n│  deltacat/utils/ —— pyarrow/daft/lance helpers [#19]│\n└─────────────────────────────────────────────────────┘\n```\n\n从 [#1] 开始跟,[#20] 是收束总结。\n\n---\n\n## 关键类比(记住这些就好)\n\n| DeltaCat 概念 | 日常类比 | 一句话 |\n|---|---|---|\n| **Catalog** | 图书馆的**目录系统** | 存着「哪本书在哪个架子上」 |\n| **Namespace** | 图书馆的**楼层** | 给 Table 分组 |\n| **Table** | 一本**多版本的书** | 名字会变,内容分版本 |\n| **TableVersion** | 书的**某一版印刷** | 定了 schema(目录结构) |\n| **Stream** | 书的**一种装订格式** | 比如 deltacat 格式 vs iceberg 格式 |\n| **Partition** | 书的**一章** | 按某个 key 分片存放 |\n| **Delta** | 一次**写入提交** | 像 git commit,按序号排列 |\n| **Manifest** | commit 里的**文件清单** | 列出这次写了哪些文件 |\n\n---\n\n## 阶段 1 · 用户代码入口\n\n### 看见了什么\n\n[#2] · [#3]\n\n`import deltacat as dc` 后,你能用的顶层函数来自两个文件:`catalog/` 包导出的 `write_to_table` / `read_table` / `create_namespace` ……,和 `api.py` 导出的更底层的 `put` / `get` / `list` / `copy`(URL 风格的)。两套 API 最终都会落到同一个 Catalog 实现。\n\n## 阶段 2 · Catalog 层:三层套娃\n\n### 为什么要三层?\n\n[#4] · [#5] · [#6]\n\n读者通常看到的调用链是:\n\n```\n用户 dc.write_to_table(...)        # 导入入口 [#4]\n  └─ catalog/delegate.py           # 路由:找当前 catalog [#5]\n       └─ catalog/main/impl.py     # 真正的写流程 [#6]\n```\n\n为什么要有 **delegate.py** 这一层?因为 DeltaCat 允许同一个进程里挂多个 catalog(比如 tutorial / prod / iceberg),`delegate` 负责根据 `catalog=」xxx」` 参数找到对应实现。\n\n## 阶段 3 · Storage 层:五级模型\n\n### 为什么分这么细?\n\n[#7] · [#8] · [#9] · [#10] · [#11] · [#12] · [#13] · [#14] · [#15]\n\n五级:`Namespace → Table → TableVersion → Stream → Partition → Delta`,再加一个每次写入产生的 `Manifest`。这些类都在 `deltacat/storage/model/*.py`。每个类都继承自 `Metafile`(就是一个 dict 加上 UUID 和持久化接口)。\n\n**设计哲学**(看 [#7] 的 README):**不可变 ID 目录** + **可变名字解析目录** 分离。Table 改名字不用动任何数据,只要在名字解析目录加一条新记录指向同一个 UUID 就行 —— 这是 DeltaCat 能做到「**重命名零开销**」的核心。\n\n## 阶段 4 · Types 层\n\n[#16] · [#17]\n\n两个 enum 非常关键,别混:\n- `ContentType` = **磁盘上的文件格式**(PARQUET, LANCE, CSV ……)\n- `DatasetType` = **内存里的数据结构类型**(pandas.DataFrame, polars.DataFrame, daft.DataFrame ……)\n\n一次 read: `ContentType.PARQUET`(磁盘) → DeltaCat → `DatasetType.DAFT`(内存 daft.DataFrame)。\n\n## 阶段 5 · IO / Utils 层\n\n[#18] · [#19]\n\n真正负责打开文件、序列化、反序列化的地方。Storage 层只关心「有一个文件清单」,不关心文件内部结构;IO/Utils 层把 pyarrow / daft / polars / lance 这些具体库藏在这里。\n\n---\n\n## 调参速查表\n\n| 我想…… | 怎么做 | 回到 tour |\n|---|---|---|\n| 换写入格式(比如从 Parquet 换成 Lance) | `content_type=ContentType.LANCE` | [#16] |\n| 换读出后的内存类型 | `read_as=DatasetType.POLARS` | [#17] |\n| 用多个 catalog(local + s3) | `dc.put_catalog(「name」, Catalog(...))`,调用时 `catalog=「name」` | [#5] |\n| 看磁盘上实际长什么样 | 打开 `catalog/` 根目录,对照 [#7] 的目录结构 | [#7] |\n\n## 我看到这个报错怎么办\n\n| 现象 | 根因 | 跳到 |\n|---|---|---|\n| `TableAlreadyExistsError` | `mode=CREATE` 但表已存在 | [#6] |\n| `DeltacatCatalog not initialized` | 忘了调用 `dc.init()` | [#4] |\n| 写进去的数据读不出来 | `ContentType` 和 `DatasetType` 不兼容(读 Lance 文件用 pandas) | [#16] [#17] |\n\n---\n\n## 读完以后你应该能回答\n\n1. 用户调 `dc.write_to_table` 后,代码依次经过哪 3 层?(答:Catalog 层 → Storage 模型 → IO/Utils)\n2. Namespace / Table / TableVersion / Stream / Partition / Delta 这六个概念谁包含谁?(看 [#7] 的 ASCII 树)\n3. Manifest 和 Delta 有什么区别?(Delta 是「一次写入操作」的元数据,Manifest 是「这次写入落了哪些文件」的清单 —— Delta 里**包含一个** Manifest)\n4. ContentType 和 DatasetType 分别描述什么?(磁盘 vs 内存)\n\n如果能流畅答出上面 4 条,这章就通关了。",
  "steps": [
    {
      "title": "导读: 从哪里读起",
      "description": "**欢迎!** 这是 DeltaCat 框架分层总览。\n\n**你是谁:** 刚入门的大一同学,之前没见过「数据湖引擎」(data lake engine) 这种东西。\n\n**这个项目在干什么:** DeltaCat 给海量文件(几百 TB 的 Parquet / Lance / CSV……)套上一层「**目录 + 版本 + 事务**」,让你能像操作数据库一样操作它们 —— 但数据实际仍存在 S3 / 本地磁盘上的普通文件里。\n\n**这一章讲什么:** 只讲「**分层架构**」。我们不讲 compaction 具体怎么实现,也不讲 Lance 怎么集成(那些有另外的章节)。读完这章你应该能画出一张 5 层图。\n\n**怎么读:**\n\n1. 强烈建议先打开左侧 CodeTour 面板里「**View Overview**」的那个按钮,把 overview 看一遍 —— 里面有 ASCII 架构图和类比表。\n2. 然后按 `[#1]` → `[#2]` → …… 顺序点过来。每一步都短,3 分钟一步。\n3. 每一步的描述分三层: **做什么 → 关注逻辑/变量 → 本次取值**。你可以只看第一行(是一句结论),也可以展开细读。\n4. 描述里的 `[#5]` 这种蓝字可以点 —— 会跳到对应的步骤,用来表达「在 X 步定义,在 Y 步被用到」。\n\n**本次的「典型例子」:** 我们用 `experiments/01_write_local.py` 的写入场景当 this-run 的参考 —— 创建一个叫 `mecka` 的 namespace,里面一个 `feb2_small_batch` 表,`content_type=LANCE`,`mode=CREATE`。之后步骤提到「本次取值」时就指这个例子。\n\n准备好了就往下走 → [#2]",
      "tags": ["导读", "role:entry"]
    },
    {
      "file": "deltacat/__init__.py",
      "line": 5,
      "title": "Entry: 用户 `import deltacat as dc` 看到什么",
      "tags": ["Entry", "role:entry", "priority:P1"],
      "color": "#f59e0b",
      "description": "**做什么:** 这是整个包的「**前台柜台**」。当你写 `import deltacat as dc` 后,所有能从 `dc.xxx` 点出来的名字都在这个文件里登记。\n\n**关注逻辑:** 这个 `__init__.py` 把散落在各个子模块的类/函数**重新导出**到顶层,让用户不用关心内部目录结构。分 4 类:\n\n1. **URL 风格 API**(`api.py`): `copy`, `get`, `list`, `put` —— 下一步 [#3] 看。\n2. **Catalog 风格 API**(`catalog/`): `write_to_table`, `read_table`, `create_namespace` …… —— [#4] 看。\n3. **Storage 模型类**(`storage/`): `Namespace`, `Table`, `Delta`, `Schema` …… —— [#8] 起一路看。\n4. **Type 枚举**(`types/media.py`): `ContentType`, `DatasetType` —— [#16]、[#17] 看。\n\n**关注变量:**\n\n| 名字 | 哪里来 | 作用 |\n|---|---|---|\n| `write = write_to_table` | 本文件第 114 行 | 给 `write_to_table` 起了短别名,所以 `dc.write(...)` 和 `dc.write_to_table(...)` 是同一个函数 |\n| `read = read_table` | 本文件第 115 行 | 同上 |\n| `__version__` | 本文件第 129 行 | 包版本,本次是 `「2.0.0.post9」` |\n\n**本次取值:** 打开 python REPL,`import deltacat as dc` 后 `dir(dc)` 能看到大约 60+ 个名字 —— 全部来自这个文件的 import 语句。`experiments/01_write_local.py` 第 17 行用的就是这个入口。"
    },
    {
      "file": "deltacat/api.py",
      "line": 97,
      "location": "copy",
      "title": "Entry: `api.py` 的 URL 风格 API",
      "tags": ["Entry", "role:dispatch", "priority:P2"],
      "color": "#3b82f6",
      "description": "**做什么:** 这个文件提供一套**基于 URL 的** API —— `copy(src, dst)` / `get(url)` / `put(url, data)` / `list(url)`,风格很像 `cp` / `cat` / `ls` 这类 shell 命令。\n\n**关注逻辑:** DeltaCat 有两套用户 API,各有场景:\n\n- **URL API**(这里):`dc.put(「dc://mycatalog/ns/tbl/」, data)` —— 像文件系统。适合做跨 catalog 搬迁、命令行工具(`dcat cp ...`)。\n- **Catalog API**([#4]):`dc.write_to_table(data, table=「tbl」, namespace=「ns」)` —— 像数据库函数。日常写代码用这个。\n\n**关注变量:**\n\n| 名字 | 出现位置 | 作用 |\n|---|---|---|\n| `src`, `dst` | `copy(...)` 参数 | 源 / 目标 URL,形如 `dc://catalog/ns/tbl/` 或 `json+s3://bucket/path/` |\n| `shallow` | `copy` 参数 | True = 只复制 metadata(不搬数据);False = 连数据一起搬 |\n| `DeltaCatUrl` | 导入自 `utils/url.py` | 解析 URL 字符串的类,见 [#19] |\n\n**本次取值:** 本次的 `experiments/01_write_local.py` **没有用** URL API,它走的是 `dc.write_to_table(...)` —— 所以这一步只是让你知道这套 API **存在**,并不会详细跟。下一步 [#4] 我们正式进入 Catalog 层。\n\n(文件头部第 70–94 行有大段注释,给了几个 URL 写法的例子,值得扫一眼再往下走。)"
    },
    {
      "file": "deltacat/catalog/__init__.py",
      "line": 1,
      "title": "Catalog 层: 公共入口 — 谁暴露给用户",
      "tags": ["Catalog 层", "role:entry", "priority:P1"],
      "color": "#f59e0b",
      "description": "**做什么:** Catalog 层是 DeltaCat 的**「门面」**(facade)。用户看到的所有表级 API —— `create_table` / `write_to_table` / `read_table` / `list_namespaces` —— 都从这里暴露。\n\n**关注逻辑:** 这个 `__init__.py` 本身不写逻辑,它只做**重新导出**,把两个子模块组合起来:\n\n1. `catalog/delegate.py` —— 提供所有表/namespace 的**顶层函数**(`write_to_table`, `read_table` ……)。\n2. `catalog/model/catalog.py` + `catalog/model/properties.py` —— 提供 `Catalog` 和 `CatalogProperties` 这两个类,用来在一个进程里注册多个 catalog。\n\n**类比:** 想象一座图书馆:\n- 「Catalog」(目录)= 图书馆自己。每个图书馆有自己的目录卡片柜。\n- 用户用 `dc.put_catalog(「tutorial」, Catalog(...))` 注册一个新目录,然后 `dc.write_to_table(..., catalog=「tutorial」)` 指定往这个目录写。\n\n**关注变量:**\n\n| 名字 | 从哪里来 | 作用 |\n|---|---|---|\n| `dcat` | 第 41 行 `from deltacat.catalog.main import impl as dcat` | **默认** catalog 实现(本地文件系统)。[#6] 会进去细看 |\n| `Catalog` | 第 33 行 | 一个 catalog 的**实例对象**(名字 + 实现 + 配置) |\n| `CatalogProperties` | 第 36 行 | 一个 catalog 的**配置**(主要是 `root` 根目录路径) |\n\n**本次取值:** `experiments/01_write_local.py` 第 106–108 行:\n```python\ndc.init()\ncat_props = CatalogProperties(root=CATALOG_ROOT)\ndc.put_catalog(「tutorial」, catalog=Catalog(config=cat_props))\n```\n就是通过这个入口注册了一个叫 `tutorial` 的 catalog,根目录是本地的 `/mnt/amlfs-03/.../catalog`。"
    },
    {
      "file": "deltacat/catalog/delegate.py",
      "line": 51,
      "location": "write_to_table",
      "title": "Catalog 层: `write_to_table` 的**路由器**",
      "tags": ["Catalog 层", "role:dispatch", "priority:P1"],
      "color": "#f59e0b",
      "description": "**做什么:** 这是用户调 `dc.write_to_table(...)` 时**第一个进入**的函数。它的任务很简单:**找到目标 catalog,把请求转发给它的 impl**。\n\n**关注逻辑:** 三步:\n\n1. 检查参数冲突(第 88–91 行):不能同时指定 `transaction=` 和 `catalog=`。\n2. 找出要操作哪个 catalog(第 95–98 行):优先看 `catalog=` 参数,否则从当前 transaction 里拿,否则用默认。\n3. **转发**(第 99–110 行):`catalog_obj.impl.write_to_table(...)` —— 这里的 `impl` 是**具体 catalog 实现**,见 [#6]。\n\n**为什么要多这一层路由?** 因为 DeltaCat 支持**多种 catalog 后端**:\n- 默认的 `catalog/main/impl.py`(本地文件系统 / S3)\n- 可选的 Iceberg catalog(`experimental/catalog/iceberg/`)\n- 用户也能写自己的 catalog impl。\n\n`delegate.py` 这一层就是**让用户代码不感知后端切换** —— 你写 `dc.write_to_table(...)`,到底落在 DeltaCat 格式还是 Iceberg 格式,由 `catalog=」xxx」` 决定,代码不用变。\n\n**关注变量:**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `data: Dataset` | 第 52 行 | 要写的数据。**可以是 None**(见本次取值) |\n| `table, namespace` | 参数 | 目标表的坐标 |\n| `mode: TableWriteMode` | 默认 `AUTO` | 写模式 —— AUTO / CREATE / APPEND / REPLACE / MERGE / DELETE |\n| `content_type: ContentType` | 默认 `PARQUET` | 落盘用什么格式。参考 [#16] |\n| `catalog_obj` | 第 98 行 `get_catalog(catalog)` | **路由结果** —— 拿到哪个 Catalog 对象 |\n\n**本次取值:** 在 `experiments/01_write_local.py` 第 119–127 行,用户这样调:\n```python\ndc.write_to_table(\n    data=None,                           # 没有 in-memory 数据!\n    table=「feb2_small_batch」,\n    namespace=「mecka」,\n    manifest=Manifest.of(entries=...),   # 用 manifest 直接注册文件\n    content_type=ContentType.LANCE,\n    mode=TableWriteMode.CREATE,\n    catalog=「tutorial」,\n)\n```\n注意 `data=None` —— 这是「**Thin-Client 模式**」:文件已经被 Ray worker 写好,这里只是来 **注册** 它们的 manifest。`catalog=」tutorial」` 让第 98 行路由到前面 [#4] 注册的那个 tutorial catalog。\n\n下一步 [#6] 我们进入转发到的 impl。"
    },
    {
      "file": "deltacat/catalog/main/impl.py",
      "line": 231,
      "location": "write_to_table",
      "title": "Catalog 层: 默认实现的 `write_to_table` —— 写流程协调",
      "tags": ["Catalog 层", "role:main", "priority:P0"],
      "color": "#ef4444",
      "description": "**做什么:** **真正干活的地方**(default catalog 实现)。把用户的 `write_to_table(...)` 请求翻译成 Storage 层的 **CRUD 操作**,并管事务提交。\n\n**关注逻辑(这个函数很长,本步只看前 40 行 —— 骨架):**\n\n```\n1. 解析 namespace(默认值)           # 第 268 行\n2. 开事务 / 加入已有事务             # 第 270–272 行\n3. 验证表是否存在 + 验证 write mode   # _get_table_and_validate_write_mode [#6 下方]\n4. 分支:\n   - 不存在 → _create_table_for_write(...)  建表\n   - 已存在 → 可能调 alter_table 改 schema\n5. 真正写入数据 → stage commit\n6. (如果满足条件)触发 compaction\n7. 提交事务\n```\n\n这一步我们只需要**理解骨架**,具体每一步都有各自的 `_handle_xxx_mode` 辅助函数([第 497–651 行]的 `_handle_replace_mode` / `_handle_append_mode` / `_handle_add_mode` / `_handle_merge_delete_mode`)。\n\n**关注变量:**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `manifest` | 参数 `Optional[Any]` | 当用户已经写好文件,只想**注册**时,直接传 `Manifest`,绕过实际的文件写入 |\n| `write_transaction` | 第 271 行 | 本次写入的事务对象 —— DeltaCat 用事务保证「要么全成,要么全败」 |\n| `table_exists_flag` | 第 276 行 | 表是否已存在 —— 决定走「新建」还是「追加」分支 |\n| `table_definition` | 第 276 行 | 成功后返回的表定义 |\n\n**本次取值**(对照 `experiments/01_write_local.py` 的 CREATE + LANCE + `data=None` + `manifest=`):\n\n- `namespace`: `「mecka」`(用户显式传的,不用 default)\n- `mode`: `TableWriteMode.CREATE`\n- `content_type`: `ContentType.LANCE`\n- `table_exists_flag`: `False`(第一次写,catalog 是空的)\n- 会走 `_create_table_for_write`(第 288 行)—— 它会创建 Namespace → Table → TableVersion → Stream → Partition → Delta 这**六个层级**的元数据对象。**下面 [#9]–[#14] 逐个看这六个对象长什么样。**\n\n> **记住:** Catalog 层的 `write_to_table` 是**指挥官** —— 它不直接写字节,它调度 Storage 层对象的创建和 IO 层的落盘。"
    },
    {
      "file": "deltacat/storage/README.md",
      "line": 6,
      "title": "Storage 层: 五级元数据模型 —— 总览",
      "tags": ["Storage 层", "role:overview", "priority:P0"],
      "color": "#ef4444",
      "description": "**做什么:** 这个 README 是**整个 Storage 层的地图**。强烈建议读一遍第 1–13 行的 ASCII 层级图,它是之后所有代码的心智模型。\n\n**关注层级(第 7–12 行):**\n\n```\nNamespace    (0 or more unordered Tables)        ← 命名空间,像楼层\n  Table      (1 or more ordered TableVersions)   ← 表,像一本多版本的书\n    TableVersion  (1 or more unordered Streams)  ← 某一版(定 schema)\n      Stream      (1 or more unordered Partitions) ← 一种存储格式\n        Partition (0 or more ordered Deltas)     ← 一个分片\n          Delta   (1 or more ordered data files) ← 一次写入(像 git commit)\n```\n\n**关注变量/概念:**\n\n| 概念 | 位置(文件) | 一句话 |\n|---|---|---|\n| Namespace | `model/namespace.py` [#9] | 最外层的分组 |\n| Table | `model/table.py` [#10] | 名字可改,多版本 |\n| TableVersion | `model/table_version.py` [#11] | 每版一个 schema |\n| Stream | `model/stream.py` [#12] | 一个存储 format(deltacat / iceberg ……) |\n| Partition | `model/partition.py` [#13] | 按 partition key 切分 |\n| Delta | `model/delta.py` [#14] | **一次** 写入。像 git commit |\n| Manifest | `model/manifest.py` [#15] | 一次写入产生的**文件清单** |\n\n**设计哲学(重要,读这行两次):** 看 README 第 26–35 行 —— DeltaCat 把「**不可变 ID 目录**」(UUID 命名,从不改)和「**可变名字解析目录**」(名字的 SHA-1 哈希命名,随改名增删)**分开存**。\n\n**为什么这样设计?** Table 改名字只要在「名字解析目录」里加一条 DELETE 旧名 + CREATE 新名的记录就行,**UUID 目录一个字节都不用动**。数据搬迁零开销。\n\n**本次取值:** 对照 `experiments/01_write_local.py`,写完之后 catalog 根目录长这样(参照 README 第 231–278 行的完整例子):\n- `{CATALOG_ROOT}/<namespace_uuid>/` ← mecka 的 UUID 目录\n- `{CATALOG_ROOT}/<sha1(mecka)>/…` ← 「mecka」这个**名字**指向上面那个 UUID 的解析目录\n- 每层下面都有 `rev/` 保存 metadata 的修订记录\n\n下一步 [#8] 进入具体代码。"
    },
    {
      "directory": "deltacat/storage/model",
      "title": "Storage 层: `storage/model/` 目录总览",
      "tags": ["Storage 层", "role:overview", "priority:P2"],
      "color": "#3b82f6",
      "description": "**做什么:** 这是 Storage 层**所有元数据类**的家。[#7] README 里提到的每个概念,对应这里的一个 `.py` 文件。\n\n**关注结构:**\n\n```\ndeltacat/storage/model/\n├── metafile.py       ← 所有元数据对象的基类 Metafile(继承 dict)\n├── locator.py        ← Locator:给对象定位的「坐标」抽象\n├── namespace.py      ← Namespace 类  [#9]\n├── table.py          ← Table 类      [#10]\n├── table_version.py  ← TableVersion  [#11]\n├── stream.py         ← Stream        [#12]\n├── partition.py      ← Partition     [#13]\n├── delta.py          ← Delta         [#14]\n├── manifest.py       ← Manifest (不是 Metafile!是写入文件的清单) [#15]\n├── schema.py         ← Schema / Field\n├── transaction.py    ← 事务读写\n├── transform.py      ← 分区变换函数(Bucket / Hour / Day / ...)\n├── sort_key.py       ← 排序键\n├── shard.py          ← 读时分片\n└── scan/, expression/ ← 读时过滤表达式\n```\n\n**关注变量(贯穿整个 Storage 层的两个基类):**\n\n| 类 | 位置 | 作用 |\n|---|---|---|\n| `Metafile` | `metafile.py` 第 470 行 | **所有元数据对象的基类**。本质是一个 dict,带 UUID 和持久化(read/write mpk 文件)能力 |\n| `Locator` | `locator.py` | **定位一个 Metafile**。每个 Metafile 有自己的 Locator 类型(`NamespaceLocator`, `TableLocator` ……) |\n\n**设计哲学:** 每个元数据类都是 **`class Xxx(Metafile)`**,也就是一个带 UUID 的 dict。这让序列化(MessagePack 格式,`.mpk` 扩展名)非常简单 —— 就是 dict 的序列化。\n\n接下来 [#9]–[#15] 我们**按层级顺序**看每个类的 `of()` 工厂方法(`of()` 是 DeltaCat 里普遍的构造约定)。每一步只看 15–25 行。"
    },
    {
      "file": "deltacat/storage/model/namespace.py",
      "line": 12,
      "location": "Namespace",
      "title": "Storage 层: Namespace —— 最外层的分组",
      "tags": ["Storage 层", "role:main", "priority:P2"],
      "color": "#3b82f6",
      "description": "**做什么:** **Namespace 是层级的最顶层**。一个 catalog 里可以有多个 Namespace,每个 Namespace 下面放若干 Table。类比文件系统就是「**目录**」,类比数据库就是「**schema**」。\n\n**关注逻辑:** `Namespace.of(locator, properties)` —— 接一个 `NamespaceLocator`(里面就一个 `namespace` 字符串字段) 和可选的 properties。\n\n第 23–39 行是典型的 **Metafile 访问器模式**:`locator` / `namespace` 这些 property 底层都是 dict 字段,用 `self.get(...)` / `self[...] = ...`。这是 DeltaCat 所有 Metafile 子类的共通模式,之后你会反复看到。\n\n**关注变量:**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `locator` | 参数 + property | `NamespaceLocator`,存着 `namespace` 字符串 |\n| `properties` | 参数 | 自由形式的 `Dict[str, Any]` —— 用户可以挂任意 kv |\n| `NamespaceLocator` | 第 77 行往下 | locator 实现 —— **核心就是一个 namespace 名字** |\n\n**本次取值:** `experiments/01_write_local.py` 第 109 行:\n```python\ndc.create_namespace(「mecka」, catalog=「tutorial」)\n```\n这一行会构造一个 `Namespace`,`locator.namespace = 「mecka」`,`properties = None`,然后落盘成 `<CATALOG_ROOT>/<namespace_uuid>/rev/…/00000000000000000001_create_…mpk`(对照 [#7] README 的完整目录结构)。"
    },
    {
      "file": "deltacat/storage/model/table.py",
      "line": 21,
      "location": "Table",
      "title": "Storage 层: Table —— 一本多版本的「书」",
      "tags": ["Storage 层", "role:main", "priority:P2"],
      "color": "#3b82f6",
      "description": "**做什么:** Table 是**跨版本**的表身份。它存的是所有版本**共享**的信息 —— 名字、描述、所有版本通用的 properties。**具体的 schema 不在这里**,在 TableVersion 里([#11])。\n\n**关注逻辑:** 看 21–26 行的 docstring:「Tables store properties common to every table version」。所以 Table 是个**轻量**的概念,它的主要职责就是:\n\n1. 有个**可改的名字**(`name`)—— 改名时用前面 [#7] 提到的「名字解析目录」机制,不动 UUID 目录。\n2. 指向**最新**的 / **最新活跃**的 TableVersion(见 `latest_table_version` / `latest_active_table_version` 字段)。\n\n**关注变量:**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `locator: TableLocator` | 参数 | 坐标 = `(namespace, table_name)` |\n| `description` | 参数 | 人类可读描述 |\n| `properties: TableProperties` | 参数 | 一个 `Dict[str, Any]` |\n| `latest_active_table_version` | 参数 | 「活跃」版本号(string) |\n| `latest_table_version` | 参数 | 「最新」版本号(string)——可能比 active 新但未发布 |\n| `native_object` | 参数 | 外部引擎(Iceberg 等)的原生对象,互操作用 |\n\n**本次取值:** 因为 `01_write_local.py` 走的是 `mode=CREATE`,`_create_table_for_write`([#6])会构造这个对象:\n- `locator` = `TableLocator(namespace=」mecka」, name=」feb2_small_batch」)`\n- `description` = None\n- `latest_table_version` = `」1」`(第一次写,就是 version 1)\n\n下一步 [#11] 看 TableVersion —— 那里才有 schema。"
    },
    {
      "file": "deltacat/storage/model/table_version.py",
      "line": 42,
      "location": "TableVersion",
      "title": "Storage 层: TableVersion —— 固定 schema 的一版",
      "tags": ["Storage 层", "role:main", "priority:P1"],
      "color": "#f59e0b",
      "description": "**做什么:** TableVersion 是**带 schema 的**一版表。每次 schema 发生不兼容的改动就开一个新版本 —— 类比一本书的**某次印刷**,一旦印出来目录不能再改。\n\n**关注逻辑:** `TableVersion.of(locator, schema, partition_scheme, ...)` 参数最多的一个类(14 个参数,第 43–58 行)。核心是**把 `schema` 锚死**:\n\n- 一个 TableVersion = 一套 **(schema + partition_scheme + sort_scheme + content_types)**。\n- 下面挂**多个 Stream**(不同存储 format 的同一份逻辑数据)。\n\n**关注变量(只挑 6 个核心的):**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `schema: Schema` | 参数 | **这一版的列定义**。不可变(对本版本而言) |\n| `partition_scheme` | 参数 | 本版本的分区规则(按哪列 bucket / truncate / date……) |\n| `sort_scheme` | 参数 | 排序键 |\n| `content_types` | 参数 | 这一版**允许** 的落盘格式列表,如 `[ContentType.PARQUET]` |\n| `lifecycle_state: LifecycleState` | 参数 | 生命周期 —— CREATED / ACTIVE / DEPRECATED |\n| `watermark: int` | 参数 | 高水位 —— 标记到哪个 stream position 已经提交成功 |\n\n**关于 `schemas` / `partition_schemes` / `sort_schemes`(复数):** 第 53–56 行这几个参数是**历史列表** —— TableVersion 允许轻量的 schema evolution(加列、改类型),每次改都 append 到列表里,当前活跃的是列表最后一个。\n\n**本次取值:** `_create_table_for_write` 会构造:\n- `locator` = `TableVersionLocator(table_locator=」mecka/feb2_small_batch」, version=」1」)`\n- `schema` = 从 `experiments/01_write_local.py` 第 33–42 行那个 8 列的 pyarrow schema 推出来(episode_id, user_id, duration, video_bytes, ……)\n- `content_types` = `[ContentType.LANCE]`(因为用户传了 `content_type=ContentType.LANCE`)\n- `lifecycle_state` = `LifecycleState.ACTIVE`"
    },
    {
      "file": "deltacat/storage/model/stream.py",
      "line": 30,
      "location": "Stream",
      "title": "Storage 层: Stream —— 一种「存储格式」",
      "tags": ["Storage 层", "role:main", "priority:P2"],
      "color": "#3b82f6",
      "description": "**做什么:** Stream 是**一种具体的存储 format** 下的数据流。同一个 TableVersion 下,可以挂多个 Stream —— 比如一个 DeltaCat 原生格式的 Stream,一个 Iceberg 兼容的 Stream,**数据逻辑上一样,物理格式不同**。\n\n**关注逻辑:** 看 30–34 行的 docstring:「An unbounded stream of Deltas」。核心语义:\n\n- Stream 是**无界**的 —— 可以一直往里追加 Delta([#14] 讲 Delta)。\n- Stream 下挂 **Partition**([#13]),每个 Partition 按 partition key 切分。\n- 第 37 行 `of(locator, partition_scheme, state, previous_stream_id, watermark, native_object)` —— 参数里的 `previous_stream_id` 用来做**版本链**(某个 Stream 被新的替代,但老的不立刻删)。\n\n**关注变量:**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `locator: StreamLocator` | 参数 | 坐标 = (`table_version_locator`, `format`),`format` 是字符串如 `」deltacat」` |\n| `partition_scheme` | 参数 | 分区规则。**和 TableVersion 的 partition_scheme 一致**,放这里做快速查询 |\n| `state: CommitState` | 参数 | 提交状态 —— STAGED / COMMITTED / DEPRECATED |\n| `watermark: int` | 参数 | 这个 stream 的**读取高水位**,读时只看 ≤ watermark 的 Delta |\n| `native_object` | 参数 | 如果是 Iceberg Stream,这里挂原生 Iceberg Table 句柄 |\n\n**本次取值:** 对 `01_write_local.py`:\n- `locator.format` = `」deltacat」`(用默认 DeltaCat 格式)\n- `state` = `COMMITTED`(写完立刻 commit)\n- `watermark` 会随着 Delta 写入不断更新"
    },
    {
      "file": "deltacat/storage/model/partition.py",
      "line": 53,
      "location": "Partition",
      "title": "Storage 层: Partition —— 按 key 切分的一块",
      "tags": ["Storage 层", "role:main", "priority:P2"],
      "color": "#3b82f6",
      "description": "**做什么:** Partition = Stream 里按 partition key 切出的**一块**。举例:如果你按 `date` 分区,那每天就是一个 Partition。**Partition 是读取的最小并行粒度**。\n\n**关注逻辑:** `Partition.of(locator, content_types, state, previous_stream_position, previous_partition_id, stream_position, partition_scheme_id, compaction_round_completion_info)` —— 8 个参数,重点理解前 4 个:\n\n- **`locator` = (`stream_locator`, `partition_values`, `partition_id`)** —— 其中 `partition_values` 是这个分区的**具体 key 值**(比如 `[「2025-01-01」, 「US」]`)。\n- **`content_types`** —— 这个分区允许的文件格式。通常和 TableVersion 一致。\n- **`state`** —— STAGED / COMMITTED。\n- **`stream_position`** —— 一个单调递增的 int,每写入一个 Delta 就 +1,用来**给 Delta 定序**。\n\n第 72–76 行有个小细节:没有 partition values(未分区表)时,`partition_scheme_id` 会被强制设成 `UNPARTITIONED_SCHEME_ID` —— 统一处理。\n\n**关注变量:**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `locator.partition_values` | `PartitionLocator` | 这个分区的 key 值 |\n| `stream_position` | 参数 | **Delta 定序用** 的 int。每次写入 +1 |\n| `state: CommitState` | 参数 | 提交状态 |\n| `compaction_round_completion_info` | 参数 | 上次 compaction 完成信息 —— 用来增量 compact |\n\n**本次取值:** `01_write_local.py` **没有**指定 partition scheme → 整个表是 **未分区**(unpartitioned) → 只会有**一个** Partition,`partition_values = None`,`partition_scheme_id = UNPARTITIONED_SCHEME_ID`,`stream_position = 1`(第一次写入)。\n\n下一步 [#14] 看 Delta —— 才是**真正的数据写入单元**。"
    },
    {
      "file": "deltacat/storage/model/delta.py",
      "line": 43,
      "location": "Delta",
      "title": "Storage 层: Delta —— 一次「写入 commit」",
      "tags": ["Storage 层", "role:main", "priority:P0"],
      "color": "#ef4444",
      "description": "**做什么:** **Delta 是整个 DeltaCat 最核心的概念**。一次 `write_to_table` 成功,就产生**一个 Delta**。类比 **git commit** —— 不可变,有序,可回溯。\n\n**关注逻辑:** `Delta.of(locator, delta_type, meta, properties, manifest, previous_stream_position)` —— 6 个字段,每个都重要:\n\n| 字段 | 类比 | 作用 |\n|---|---|---|\n| `locator` | git commit hash | 唯一标识 (partition + stream_position) |\n| `delta_type` | commit 类型 | APPEND / UPSERT / DELETE |\n| `meta: ManifestMeta` | commit metadata | 数据行数、字节数、schema_id |\n| `properties` | commit tags | 自由 k/v |\n| `manifest: Manifest` | **commit 里改了哪些文件** | 文件清单,见 [#15] |\n| `previous_stream_position` | git parent | 指向上一个 Delta,形成链 |\n\n**为什么 Delta 要不可变?** 一旦写进去就不能改,和 git 一样。要修改数据?写**一个新的 Delta**(UPSERT type),读取时引擎会把所有 Delta 按 stream_position 排序,依次应用 —— 这就是 **LSM-Tree 思想**,也是数据湖(Delta Lake / Iceberg / Hudi)的通用模型。\n\n**合并:** 第 67 行往下的 `Delta.merge_deltas(...)` —— 把多个 Delta 合成一个(用于 compaction)。类比 Time Machine 合并快照,或 git squash。\n\n**关注变量:**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `stream_position` | `locator` 里 | Delta 的序号,**决定应用顺序** |\n| `manifest` | 字段 | **这个 Delta 包含的文件清单**,下一步 [#15] 展开 |\n| `previous_stream_position` | 字段 | 上一个 Delta 的 stream_position,形成链 |\n\n**本次取值:** `01_write_local.py` 用 `mode=CREATE`,所以:\n- `delta_type` = APPEND(创建时默认)\n- `stream_position` = 1(第一个 Delta)\n- `previous_stream_position` = None\n- `manifest` = `Manifest.of(entries=merged)` —— 用户直接把构造好的 manifest 传进来了(Thin-Client 模式)\n\n那个 `merged` 里是 Lance 文件的 ManifestEntry 列表,[#15] 会看它的结构。"
    },
    {
      "file": "deltacat/storage/model/manifest.py",
      "line": 96,
      "location": "Manifest",
      "title": "Storage 层: Manifest —— 一次写入的「文件清单」",
      "tags": ["Storage 层", "role:main", "priority:P0"],
      "color": "#ef4444",
      "description": "**做什么:** **Manifest 是一个 Delta 对应的文件列表**。每个 Delta 都带一个 Manifest,Manifest 里列出「**这次写入产生了哪些磁盘文件,每个文件在哪、多大、什么格式**」。\n\n**类比:** git commit 有一个 「files changed」列表 —— Manifest 就是 Delta 的 files-changed。\n\n**关注逻辑:** Manifest 本身是个简单的 **dict**(第 96 行 `class Manifest(dict)`),结构:\n\n```python\n{\n  「id」: 「uuid」,              # 这个 manifest 的 id\n  「meta」: ManifestMeta(...),  # 汇总元数据(总行数 / 字节数 / schema_id / content_type)\n  「entries」: ManifestEntryList([\n      ManifestEntry(url=「...」, meta=ManifestMeta(...), mandatory=True, id=「...」),\n      ManifestEntry(url=「...」, ...),\n      ...\n  ]),\n  「author」: ManifestAuthor(name=「...」, version=「...」),\n}\n```\n\n**关注三个子类:**\n\n| 类 | 定义行 | 作用 |\n|---|---|---|\n| `Manifest` | 96 | 容器,有 `meta` + `entries` + `author` |\n| `ManifestMeta` | 290 | **每个文件**的元数据:record_count / content_length / source_content_length / content_type / schema_id / partition_values |\n| `ManifestEntry` | 406 | **一个文件**的条目:`url`(必填)+ `meta` + `mandatory` + `id`。第 406–429 行你能看到 `of()` 里对 `url` / `uri` 的兼容逻辑 |\n| `ManifestEntryList` | 636 | `list[ManifestEntry]` 的子类,支持 **lazy 转换**(第 646–654 行的 `__getitem__` / `__iter__`)|\n\n**为什么要有 Manifest 这一层?** 如果数据已经在磁盘上(比如 Ray worker 们并行写完了 Lance 文件),这时不需要 DeltaCat **再写一遍** —— 直接把文件清单塞进 Manifest,DeltaCat 只做「**注册**」即可。**这就是 Thin-Client 模式**,也是 `experiments/01_write_local.py` 使用的模式。\n\n**本次取值:** 看 `01_write_local.py` 第 113–117 行:\n```python\nfor r in results:\n    entries = build_manifest_entries(lance_path=r[「lance_path」])\n    all_entries.extend(entries)\n\nmerged = ManifestEntryList.of(all_entries)\n```\n每个 Ray worker 写一个 `.lance` 文件夹(Lance 是目录而不是单文件),`build_manifest_entries` 扫描这个目录得到多个 `ManifestEntry`(**一个 Lance 数据集 = 多个 ManifestEntry**,见 `utils/lance_utils.py`)。346 个 episode → 若干千个 ManifestEntry → 最后合成**一个** Manifest → 挂到**一个** Delta 上 → 提交。"
    },
    {
      "file": "deltacat/types/media.py",
      "line": 7,
      "location": "ContentType",
      "title": "Types 层: `ContentType` —— 磁盘上的文件格式",
      "tags": ["Types 层", "role:main", "priority:P1"],
      "color": "#f59e0b",
      "description": "**做什么:** `ContentType` 枚举告诉 DeltaCat:**写到磁盘上的文件长什么样**。Parquet?CSV?Lance?这个 enum 就是那套「菜单」。\n\n**关注逻辑:** 本质是 MIME type 的一个受限集合(见 28–34 行,用 IANA 格式如 `」application/parquet」`)。重点看分类集合(第 91–124 行):\n\n| 集合 | 含义 | 包含 |\n|---|---|---|\n| `SCHEMA_CONTENT_TYPES` | **自带 schema** 的格式 | PARQUET, ORC, FEATHER, AVRO, **LANCE** |\n| `DELIMITED_TEXT_CONTENT_TYPES` | 文本分隔符格式 | CSV, TSV, PSV, UNESCAPED_TSV |\n| `TABULAR_CONTENT_TYPES` | **能当表用**的(上面两个的并集 + 一些) | 几乎所有 |\n| `EXPLICIT_COMPRESSION_CONTENT_TYPES` | **需要外部压缩**的(文本类) | CSV, TSV, PSV, JSON |\n\n**关注变量:**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `ContentType.PARQUET` | 第 30 行 | 默认格式 |\n| `ContentType.LANCE` | 第 28 行 | 这份仓库重点集成的格式(向量数据库友好) |\n| `EXT_TO_CONTENT_TYPE` | 第 59 行 | 文件扩展名 → ContentType 映射。读文件时自动推断 |\n| `CONTENT_TYPE_TO_EXT` | 第 72 行 | 反向 —— 写文件时决定扩展名 |\n\n**记住这一条:** `ContentType` = **磁盘**上长什么样。下一步 [#17] 的 `DatasetType` = **内存**里长什么样。两个不同轴。\n\n**本次取值:** `experiments/01_write_local.py` 第 19 行 `from deltacat.types.media import ContentType`,第 124 行 `content_type=ContentType.LANCE`,意思是「落盘用 Lance 格式」。这会被 Catalog 层记到 TableVersion 的 `content_types` 列表里([#11])。"
    },
    {
      "file": "deltacat/types/media.py",
      "line": 127,
      "location": "DatasetType",
      "title": "Types 层: `DatasetType` —— 内存里的数据结构",
      "tags": ["Types 层", "role:main", "priority:P1"],
      "color": "#f59e0b",
      "description": "**做什么:** `DatasetType` 告诉 DeltaCat:**读出来后内存里是哪种对象**。pandas.DataFrame?pyarrow.Table?daft.DataFrame?\n\n**关注逻辑:** 第 127–146 行定义了两组:\n\n```\n# 本地 (单机)\nLANCE            → lance.LanceDataset (lazy!随机访问 O(1))\nNUMPY            → numpy.ndarray\nPANDAS           → pandas.DataFrame\nPOLARS           → polars.DataFrame\nPYARROW          → pyarrow.Table\nPYARROW_PARQUET  → pyarrow.parquet.ParquetFile\n\n# 分布式 (多机)\nDAFT             → daft.DataFrame\nRAY_DATASET      → ray.data.Dataset\n```\n\n注意 `LANCE` 是**本地**类型 —— Lance 数据集用 lazy loading,不把全部数据读进内存。\n\n**分组方法**(第 148–163 行):\n```python\nDatasetType.local()        # {NUMPY, PANDAS, POLARS, PYARROW, PYARROW_PARQUET}\nDatasetType.distributed()  # {DAFT, RAY_DATASET}\n```\n\n第 165 行开始的 `readable_content_types()` —— 每种 DatasetType **能读哪些 ContentType**。比如 DAFT 能读 PARQUET / JSON / CSV,但不能直接读 AVRO。\n\n**关注变量:**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `DatasetType.DAFT` | 第 145 行 | `read_table` 的**默认** 读取类型(见 [#5] `read_table` 的默认参数) |\n| `DatasetType.local()` | 第 155 行 | 本地类型集合 |\n| `DatasetType.distributed()` | 第 148 行 | 分布式类型集合 |\n\n**ContentType vs DatasetType 对照记忆:**\n\n| | ContentType | DatasetType |\n|---|---|---|\n| 在哪 | **磁盘** | **内存** |\n| 例子 | PARQUET, LANCE, CSV | PANDAS, POLARS, DAFT |\n| 用来 | `write_to_table(content_type=...)` 决定落盘格式 | `read_table(read_as=...)` 决定返回对象类型 |\n\n**本次取值:** `01_write_local.py` 没有读操作,所以没碰 `DatasetType`。但如果接着跑 `02_read_local.py`,你会看到 `read_as=DatasetType.DAFT` 或 `DatasetType.PYARROW`。"
    },
    {
      "directory": "deltacat/io",
      "title": "IO 层: `deltacat/io/` —— 真正和 Ray Data 对接的地方",
      "tags": ["IO 层", "role:overview", "priority:P2"],
      "color": "#3b82f6",
      "description": "**做什么:** 这个目录负责**把 DeltaCat 接入 Ray Data 的读写体系**。Ray Data 的读写约定是:`Datasource` / `Datasink` / `Dataset` 三件套,分别管「读源」「写汇」「数据集」。\n\n**关注结构:**\n\n```\ndeltacat/io/\n├── __init__.py               ← 导出 read_deltacat / DeltacatReadType\n├── reader/\n│   └── deltacat_read_api.py  ← read_deltacat() 公共入口\n├── datasource/\n│   └── deltacat_datasource.py ← DeltacatDatasource (给 Ray Data 的「读源」)\n├── datasink/\n│   └── deltacat_datasink.py   ← DeltacatDatasink  (给 Ray Data 的「写汇」)\n├── dataset/\n│   └── deltacat_dataset.py    ← DeltacatDataset   (包装 ray.data.Dataset)\n└── *_object_store.py          ← Redis / Memcached / Plasma / S3 等 object store 适配\n```\n\n**关注变量:**\n\n| 名字 | 位置 | 作用 |\n|---|---|---|\n| `read_deltacat(...)` | `reader/deltacat_read_api.py` | **读** 一组 DeltaCat URL,返回 Ray Dataset |\n| `DeltacatReadType` | `datasource/deltacat_datasource.py` | 读类型枚举:`METADATA_LIST` / `METADATA_LIST_RECURSIVE` / 数据读 |\n| `METAFILE_DATA_COLUMN_NAME` | 同上 | 读元数据时,返回 Dataset 里这一列放 metafile 序列化结果 |\n\n**和 Storage 层的关系:** Storage 层([#7]–[#15])负责**元数据**(「**哪些文件属于这个 Delta**」),**IO 层负责把这些文件实际打开、读成 DataFrame**。两层之间的接口就是 Manifest([#15])。\n\n**本次取值:** `experiments/01_write_local.py` 是 Thin-Client 模式,**Ray worker 自己用 `lance.write_dataset` 写文件**,绕开了 DeltaCat 的 IO 层。如果用 `dc.write_to_table(data=<some dataframe>, ...)`(data 不是 None),才会走到这里的 `DeltacatDatasink`。读场景([#17] 讲过)一定会走 `read_deltacat`。"
    },
    {
      "directory": "deltacat/utils",
      "title": "Utils 层: `deltacat/utils/` —— 具体格式的 helper",
      "tags": ["Utils 层", "role:overview", "priority:P2"],
      "color": "#3b82f6",
      "description": "**做什么:** 这里是**各种数据格式的具体读写工具**。IO 层([#18])是「**怎么接 Ray Data**」的框架层,这里是「**具体 pyarrow / pandas / lance 怎么调**」的实现层。\n\n**关注结构(按重要程度):**\n\n```\ndeltacat/utils/\n├── pyarrow.py      (~2000 行) ← PyArrow 读写、schema 转换;最厚实的一个\n├── daft.py         (~1000 行) ← Daft DataFrame 读写\n├── lance_utils.py  (~700 行)  ← Lance 读写 + build_manifest_entries\n├── polars.py                   ← Polars 读写\n├── pandas.py                   ← Pandas 读写\n├── numpy.py                    ← NumPy 读写\n├── url.py          (~1400 行) ← DeltaCatUrl 解析器([#3] 提到的 URL 核心)\n├── filesystem.py   (~1600 行) ← pyarrow.fs 的 wrapper,给 S3 / 本地统一接口\n├── schema.py                   ← 跨库 schema 转换(pyarrow ↔ pandas ↔ polars)\n├── export.py                   ← 导出到其他格式\n├── metafile_locator.py         ← 辅助 Metafile 定位\n├── resources.py                ← Ray 资源规划\n├── performance.py              ← timing / profiling helpers\n├── metrics.py                  ← 指标上报\n├── cloudpickle.py              ← pickle 增强\n├── reader_compatibility_mapping.py  ← 「这个 ContentType + DatasetType 组合能不能读」\n├── common.py, arguments.py     ← 杂项\n└── ray_utils/                  ← Ray 相关的 runtime / placement utilities\n```\n\n**关注变量(从大局视角):**\n\n| helper 文件 | 承担的职责 | 对应 DatasetType / ContentType |\n|---|---|---|\n| `pyarrow.py` | PyArrow 读写 | DatasetType.PYARROW / ContentType.PARQUET 等 |\n| `daft.py` | Daft 读写 | DatasetType.DAFT |\n| `lance_utils.py` | Lance 读写、manifest 构造 | DatasetType.LANCE / ContentType.LANCE |\n| `reader_compatibility_mapping.py` | 兼容性矩阵 | 决定「这个组合能不能读」 |\n\n**关键函数:** `lance_utils.py` 里的 **`build_manifest_entries(lance_path=...)`** —— 就是 `experiments/01_write_local.py` 第 22、113 行用的那个函数,把一个 `.lance` 目录扫描成多个 ManifestEntry([#15])。**这是 Lance 集成的核心入口**,但详细讲解在另一份 tour。\n\n**记忆要点:** 你看到代码里有 `from deltacat.utils.xxx import ...`,都是在**具体用某种库的 API**。Storage / Catalog 层是**抽象协议**,这里是**具体实现**。"
    },
    {
      "title": "收束: 请求从哪来到哪去",
      "tags": ["收束", "role:return"],
      "description": "**恭喜!** 这章走完了。现在做一次**心智复盘** —— 把一次 `dc.write_to_table(...)` 在 DeltaCat 里走的完整路径过一遍:\n\n```\n┌─────────────────────────────────────────────────────────┐\n│ 用户代码                                                │\n│   import deltacat as dc           ← [#2]                │\n│   dc.write_to_table(data, ...)                          │\n└─────────────────────┬───────────────────────────────────┘\n                      │\n┌─────────────────────▼───────────────────────────────────┐\n│ Catalog 层 (目录层)                                     │\n│   catalog/__init__.py         ← 公共 API 导出 [#4]      │\n│   catalog/delegate.py         ← 找 catalog 并转发 [#5]  │\n│   catalog/main/impl.py        ← 协调写流程 [#6]         │\n└─────────────────────┬───────────────────────────────────┘\n                      │\n┌─────────────────────▼───────────────────────────────────┐\n│ Storage 层 (五级元数据) —— 见 README [#7]               │\n│                                                         │\n│   Namespace  [#9]    ← 最外层分组,namespace=」mecka」  │\n│     └─ Table [#10]   ← 名字可改,table=」feb2_...」     │\n│          └─ TableVersion [#11]  ← 定 schema, ver=」1」  │\n│               └─ Stream [#12]   ← 定 format=」deltacat」 │\n│                    └─ Partition [#13]  ← 切分(本例未分区) │\n│                         └─ Delta [#14]  ← 这次写入     │\n│                              └─ Manifest [#15]          │\n│                                   ← 清单:哪些文件      │\n└─────────────────────┬───────────────────────────────────┘\n                      │\n┌─────────────────────▼───────────────────────────────────┐\n│ Types 层 (枚举)                                         │\n│   ContentType.LANCE    ← 磁盘格式 [#16]                │\n│   DatasetType.DAFT     ← 内存类型 [#17]                │\n└─────────────────────┬───────────────────────────────────┘\n                      │\n┌─────────────────────▼───────────────────────────────────┐\n│ IO/Utils 层 (真正落盘)                                  │\n│   deltacat/io/        ← Ray Data 对接 [#18]            │\n│   deltacat/utils/     ← pyarrow/daft/lance 调用 [#19]  │\n└─────────────────────────────────────────────────────────┘\n```\n\n**一句话复盘:**\n\n> 用户调 `dc.write_to_table` → Catalog 层找到目标 catalog → 根据 mode 创建 / 更新六级元数据对象(Namespace → Table → TableVersion → Stream → Partition → Delta + Manifest) → 按 ContentType 用 IO/Utils 层落盘 → 提交事务。\n\n**自测 4 题(在 overview 里也提过):**\n\n1. Namespace / Table / TableVersion / Stream / Partition / Delta 谁包含谁?\n2. Delta 和 Manifest 谁属于谁?\n3. `ContentType` vs `DatasetType` 分别是磁盘还是内存?\n4. `catalog/delegate.py` 为什么要存在?(为什么不直接调 impl?)\n\n能答出就通关了。要深入 Lance 集成看下一章(另一份 tour);要看 compaction 算法、事务提交、读时过滤 —— 各自有 deep-dive 章节。"
    }
  ]
}
